subagent-driven

---

<task-notification>
<task-id>a340dbb8fbc771173</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-protlms/bf2b7b10-35fa-4759-98a9-8aca3a54d050/tasks/a340dbb8fbc771173.output</output-file>
<status>completed</status>
<summary>Agent "Implement Task 1: contract contacts" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Status: DONE
Commit: `9e9c348` — "contract: add contacts capability + contact_map artifact (bump 0.4)" (on branch `feature/contacts-categorical-jacobian`)
Tests: `pytest tests/test_contract.py -v` → 21 passed (RED confirmed first: 3 failing before implementation); full repo suite also passes (only unrelated Docker-integration tests skipped); ruff check/format and ty check all clean.
Concerns: none blocking. Minor note — `docs/CONTRACT.md` §4 "Inputs" bullet list wasn't updated to mention `contacts` uses `/in/seqs.fasta` too, since that wasn't in the brief's explicit edit list and no test covers it; left out of scope intentionally.
Report: `/home/briney/git/protlms/.superpowers/sdd/task-1-report.md`</result>
<usage><subagent_tokens>63269</subagent_tokens><tool_uses>37</tool_uses><duration_ms>179285</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a24802bf2dab25b43</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-protlms/bf2b7b10-35fa-4759-98a9-8aca3a54d050/tasks/a24802bf2dab25b43.output</output-file>
<status>completed</status>
<summary>Agent "Review Task 1 (spec + quality)" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>This confirms everything matches the brief exactly, including the local `from protlms.contract import ArtifactKind` import inside the test function (matching the brief's exact code verbatim) despite `ArtifactKind` not being imported at module level — this is consistent with the brief's literal instructions and is a minor, harmless local-import style quirk (not introduced by the implementer's own judgment).

I have everything needed for the review.

### Spec Compliance
- ✅ Spec compliant

All required interfaces verified directly in the diff/files: `CONTRACT_VERSION = "0.4"` (`src/protlms/contract.py:18`), `Capability.CONTACTS = "contacts"` (`contract.py:28`), `ArtifactKind.CONTACT_MAP = "contact_map"` (`contract.py:48`). `tests/data/result.contacts.example.json` exists with `capability: "contacts"`, artifact `kind: "contact_map"`, and non-null `shape` fields — matching what `test_documented_contacts_result_example_validates` asserts (`tests/test_contract.py:53-58`). `docs/CONTRACT.md` was updated in lockstep: header bumped to `0.4` (line 3), `contacts` subcommand added to the CLI block (lines 59-60), capabilities table row updated (line 92), manifest worked example bumped and given `contacts` (lines 105-117), Outputs table row added (line 145), `OutputArtifact.kind` list updated (line 206), and a new "Worked example (contacts)" block added verbatim matching the JSON fixture (lines 273-289). Scope was respected: `result.embed/score/generate.example.json` and their `docs/CONTRACT.md` worked examples were correctly left at `0.3`, as the brief intended.

- ⚠️ Cannot verify from diff alone: the report's TDD RED/GREEN transcripts and the "31 files left unchanged" ruff/ty output are implementer-supplied claims not independently re-run per review instructions; taken as given per task guidance.

### Strengths
- Every named file in the brief was touched, and only those files — no scope creep.
- The `docs/CONTRACT.md` edits are a precise, line-for-line mirror of the code and fixture changes; the new worked example is byte-identical to `tests/data/result.contacts.example.json`.
- Correctly left the other three worked examples' `contract_version: "0.3"` untouched, matching explicit brief guidance not to touch them.
- New tests assert real, meaningful behavior (capability identity, artifact kind, shape presence) rather than tautologies, and mirror the brief's test code verbatim.
- Self-review in the report is honest and specific, including proactively flagging the one omitted prose section rather than hiding it.

### Issues

#### Critical (Must Fix)
None.

#### Important (Should Fix)
None.

#### Minor (Nice to Have)
- `docs/CONTRACT.md:126-130` — The "Inputs" bullet list under §4 documents which capabilities read `/in/seqs.fasta` (`embed`, `likelihood`) but was not updated to include `contacts`, even though `contacts` also consumes `/in/seqs.fasta` per the new CLI subcommand block added at line 59. This is prose with no drift-guard test coverage, and the brief's step 6 didn't explicitly list it, so it's defensible as out-of-scope — but it's a real, if small, documentation gap that a future reader could trip on. Worth a one-line addition (`contacts`: reuses `/in/seqs.fasta`) in this task or the next docs pass.
- `tests/test_contract.py:47` — `ArtifactKind` is imported locally inside `test_contacts_capability_and_artifact_kind_exist` rather than added to the module-level import block (line 9-20), which already imports `Capability`, `PoolingMode`, etc. from the same module. This matches the brief's literal snippet verbatim, so it's not a deviation, but it's inconsistent with the file's existing style of importing all contract symbols at the top. Trivial to hoist.

### Assessment
**Task quality:** Approved
**Reasoning:** All required interfaces exist with exact values, the contract/doc mirror rule was honored precisely, scope boundaries were respected, and tests assert real schema behavior with a genuine RED→GREEN cycle. The two minor findings are cosmetic/documentation nits that don't block trust in this task's correctness.</result>
<usage><subagent_tokens>49152</subagent_tokens><tool_uses>6</tool_uses><duration_ms>31865</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a2c5985bcfedfce07</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-protlms/bf2b7b10-35fa-4759-98a9-8aca3a54d050/tasks/a2c5985bcfedfce07.output</output-file>
<status>completed</status>
<summary>Agent "Implement Task 2: load_contact_maps" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Perfect! Everything is complete. Let me provide the final summary:

---

## DONE

**Status:** DONE

**Commit:** bf60a7a — "io: add load_contact_maps for contact_map artifacts"

**Tests:** 2 contact_maps tests PASS; full suite 206 passed, 29 skipped

**Changes:** src/protlms/io.py (+15 lines) | tests/test_io.py (+43 lines)

**Report:** /home/briney/git/protlms/.superpowers/sdd/task-2-report.md

Implemented `load_contact_maps(out_dir: Path, result: Result) -&gt; dict[str, np.ndarray]` following TDD: wrote failing tests → confirmed import error → added function mirroring `load_per_residue_embeddings` pattern → all tests pass. Linting/formatting/type-check clean. No concerns.</result>
<usage><subagent_tokens>48575</subagent_tokens><tool_uses>31</tool_uses><duration_ms>127103</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a7fc47684f7c9844a</task-id>
<tool-use-id>toolu_01VGkVZyr8XZfbrzT7AZ7oBo</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-protlms/bf2b7b10-35fa-4759-98a9-8aca3a54d050/tasks/a7fc47684f7c9844a.output</output-file>
<status>completed</status>
<summary>Agent "Review Task 2 (spec + quality)" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>I have everything needed. `ArtifactKind.CONTACT_MAP = "contact_map"` exists, `_artifacts` helper is confirmed, and `OutputArtifact.record_ids` is `list[str] | None`. Let me verify the implementation matches the exact code from the task brief and check for divergence from `load_per_residue_embeddings`.

The implementation matches the task brief's Step 3 code verbatim. The two tests match the brief's Step 1 code verbatim as well. Let's double check there's no other artifact loader with id-keying + fallback pattern already in the file to compare style consistency (e.g. `load_pooled_embeddings` uses `.npz` differently, that's fine since it's a different artifact type).

Everything checks out. Now finalizing the review.

### Spec Compliance
- ✅ Spec compliant

Verified point by point:
- Signature matches exactly: `load_contact_maps(out_dir: Path, result: Result) -&gt; dict[str, np.ndarray]` (`src/protlms/io.py:217`).
- Id-keying uses `artifact.record_ids[0]` with a `Path(artifact.path).stem` fallback when `record_ids` is falsy (`src/protlms/io.py:225`) — matches the brief's required fallback behavior, and `OutputArtifact.record_ids: list[str] | None` in `src/protlms/contract.py:83` makes the falsy-check safe (`None` or `[]` both fall through to stem).
- Raises `OutputParseError("result declares no contact_map artifacts")` when the filtered artifact list is empty (`src/protlms/io.py:227-228`).
- Uses `_artifacts(result, ArtifactKind.CONTACT_MAP)` (`src/protlms/io.py:224`), the same private helper used by `load_per_residue_embeddings` and `load_pooled_embeddings` — no reimplementation of filtering logic.
- Test 1 (`tests/test_io.py:95-118`, `test_load_contact_maps_returns_arrays_by_id`) is a real happy-path assertion: writes an actual `.npy` file to disk, builds a valid `Result` with a `contact_map` artifact carrying `record_ids: ["gb1"]`, then asserts both the key set (`{"gb1"}`) and the loaded array's shape (`(5, 5)`) — this exercises the `record_ids[0]` branch specifically, not just the stem fallback.
- Test 2 (`tests/test_io.py:121-133`, `test_load_contact_maps_raises_when_absent`) constructs a valid `Result` with an empty `artifacts` list and asserts `pytest.raises(OutputParseError)` — genuinely exercises the empty-artifacts guard.
- Both tests are the exact tests specified in the task brief; nothing was altered or elided.

⚠️ Nothing left unverifiable — the diff is small and self-contained, and the referenced dependencies (`ArtifactKind.CONTACT_MAP`, `_artifacts`, `OutputParseError`, `Result`) are all confirmed present in `src/protlms/contract.py` and `src/protlms/io.py` as of this diff's base.

### Strengths
- Implementation is byte-for-byte faithful to the task brief's prescribed code and correctly reuses the existing `_artifacts` private helper rather than duplicating filtering logic.
- The `record_ids[0]`-with-stem-fallback branch is actually exercised by a real `.npy` file on disk plus shape assertion, not a stub — Test 1 is a meaningful behavioral check, not a smoke test.
- Placement in `io.py` (immediately after `load_per_residue_embeddings`) and the docstring style (one-liner + `Raises:` section) match the surrounding file's conventions.
- Report accurately reflects the diff — no discrepancies found between claimed and actual changes; lint/format/type-check/full-suite results are consistent with a clean small addition.

### Issues

#### Critical (Must Fix)
None.

#### Important (Should Fix)
None.

#### Minor (Nice to Have)
- `src/protlms/io.py:225` vs `src/protlms/io.py:211`: `load_contact_maps` adds an id-resolution branch (`record_ids[0]` else stem) that `load_per_residue_embeddings` does not have (which unconditionally uses the stem). This is a deliberate, brief-mandated divergence (contact maps are pairwise and a single artifact could in principle list multiple record ids, though in practice today it's always a singleton list), not an oversight — but it does mean the function isn't a *pure* mirror of `load_per_residue_embeddings`; it's closer to a hybrid of that function's loop structure and `load_pooled_embeddings`'s `record_ids`-driven keying. Worth a one-line comment if a future reader wonders why contact maps get special id-resolution treatment when embeddings don't. Not blocking.
- No test exercises the stem-fallback branch (`record_ids` absent/empty) for `load_contact_maps` — only the `record_ids[0]` branch is covered by Test 1. This is exactly what the task brief's Step 1 specified, so it's not a deviation, but it does leave one code path (`Path(artifact.path).stem` fallback at `src/protlms/io.py:225`) without direct test coverage. Low risk since the same fallback expression is already exercised elsewhere in the file for `load_per_residue_embeddings`.

### Assessment
**Task quality:** Approved
**Reasoning:** The implementation and tests match the task brief exactly, correctly reuse existing helpers, and are verified clean by ruff/ty/pytest per the report (consistent with the diff's small, self-contained scope). Only trivial, non-blocking polish items noted.</result>
<usage><subagent_tokens>35688</subagent_tokens><tool_uses>5</tool_uses><duration_ms>28763</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>ac0b9d155bac5e934</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-protlms/bf2b7b10-35fa-4759-98a9-8aca3a54d050/tasks/ac0b9d155bac5e934.output</output-file>
<status>completed</status>
<summary>Agent "Implement Task 3: Model.contacts" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Status: DONE
Commit: c3329c4 — "models: add Model.contacts + ContactsResult"
Tests: 36/36 pass in tests/test_models.py (4 new contacts tests, TDD RED→GREEN confirmed); full repo suite passes with only docker-dependent integration tests skipped (no daemon)
Lint/format/type-check: ruff check, ruff format, ty check all clean
Concerns: `pytest`/`python` on `$PATH` resolve to a linuxbrew Python without protlms installed — had to use `/home/briney/miniforge3/envs/ai/bin/python -m pytest` explicitly (conda env `ai`). Also note: `.superpowers/sdd/task-3-report.md` had a stale, unrelated report (ProtBERT Dockerfile task) from an earlier SDD sequence — overwritten per the assigned output path, old content noted in the new report.
Report: `/home/briney/git/protlms/.superpowers/sdd/task-3-report.md`</result>
<usage><subagent_tokens>75243</subagent_tokens><tool_uses>39</tool_uses><duration_ms>177144</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a8446a2d2afaa1bd0</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-protlms/bf2b7b10-35fa-4759-98a9-8aca3a54d050/tasks/a8446a2d2afaa1bd0.output</output-file>
<status>completed</status>
<summary>Agent "Review Task 3 (spec + quality)" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>No line-length violations. All lines are within 100 chars.

### Spec Compliance
- ✅ Spec compliant

### Strengths
- `contacts` signature matches exactly: `contacts(fasta, *, method="categorical-jacobian", output_dir=None, use_gpu=False, batch_size=None) -&gt; ContactsResult` with no `chunk_size` (`src/protlms/models.py:276-284`).
- Validation order mirrors `score` precisely: `_require_capability` first, then method check raising `InvalidRequestError` before any staging/run occurs (`src/protlms/models.py:303-307`, compare `score` at `src/protlms/models.py:261-265`). Test `test_contacts_invalid_method_raises_before_run` (`tests/test_models.py:522-526`) confirms `last_spec is None` after the raise, proving no run was attempted.
- Uses `self._run(...)` directly, not `_run_chunked` (`src/protlms/models.py:312-314`) — correctly honors the YAGNI note.
- `ContactsResult.maps()` delegates to `load_contact_maps(self.output_dir, self.result)` (`src/protlms/models.py:126-128`), and `load_contact_maps` itself (pre-existing, `src/protlms/io.py:217-229`) correctly keys by `record_ids[0]` and raises `OutputParseError` if no `contact_map` artifacts exist.
- `ContactsResult` exported in both the import block (`src/protlms/__init__.py:19`) and `__all__` (`src/protlms/__init__.py:39`), positioned consistently with the other result types.
- `FakeRunner._write_contacts` (`tests/test_models.py:169-185`) emits real `(n, n)` float32 `.npy` arrays with correct `record_ids`, `shape`, and `dtype` metadata, and routes through the shared `_write_result` writer producing a valid `contacts` result.json — matches the `embed`/`generate` writer patterns exactly.
- `_manifest_json` default capabilities updated to `["embed", "likelihood", "score", "contacts"]` (`tests/test_models.py:33`); implementer verified (and I have no reason to doubt, given the diff is minimal and additive) that pre-existing tests are unaffected — no existing test asserts `contacts` is absent from the default capability list.
- The 4 new tests assert real behavior, not mocks: `test_contacts_returns_maps_by_id` checks actual returned array shapes and keys (`tests/test_models.py:502-510`); `test_contacts_builds_expected_command` inspects the real `RunSpec.command` built by `Model.contacts` (`tests/test_models.py:513-519`); the two error-path tests assert exception types and the "no run attempted" invariant via `last_spec is None`.
- `ContactsResult` dataclass shape (`result`, `output_dir`, `method`, `_keepalive` with `field(default=None, repr=False)`) is field-for-field identical to `ScoreResult` (`src/protlms/models.py:90-101` vs `117-128`), including keyword-only construction and repr suppression on `_keepalive`.
- Docstring style, args, and Raises sections for `contacts` closely parallel `score`'s docstring, correctly substituting contacts-specific error semantics.

### Issues

#### Critical (Must Fix)
None.

#### Important (Should Fix)
None.

#### Minor (Nice to Have)
None. The diff is a clean, minimal, verbatim implementation of the brief with no divergence found.

### Assessment
**Task quality:** Approved
**Reasoning:** The diff implements exactly what the brief specifies — signature, validation order, `_run` (not `_run_chunked`), result dataclass shape, export locations, and test doubles all verified line-for-line against the brief and against the parallel `score` implementation, with no deviations found.</result>
<usage><subagent_tokens>61342</subagent_tokens><tool_uses>9</tool_uses><duration_ms>34943</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a3cdcf7d014e04d64</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-protlms/bf2b7b10-35fa-4759-98a9-8aca3a54d050/tasks/a3cdcf7d014e04d64.output</output-file>
<status>completed</status>
<summary>Agent "Implement Task 4: CLI contacts" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Perfect! Everything is complete. Let me create a final summary:

---

## Status: COMPLETE

**Commits:** 
- `1ab814b cli: add protlms contacts command`

**Test Summary:** All 18 tests pass; new test `test_contacts_command_invokes_model` validates correct method and default argument handling.

**Code Quality:** Lint, format, and type checks all passing. Implementation follows established `score` command pattern.

**Report:** `/home/briney/git/protlms/.superpowers/sdd/task-4-report.md`</result>
<usage><subagent_tokens>48756</subagent_tokens><tool_uses>28</tool_uses><duration_ms>107062</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a472b820c97d62290</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-protlms/bf2b7b10-35fa-4759-98a9-8aca3a54d050/tasks/a472b820c97d62290.output</output-file>
<status>completed</status>
<summary>Agent "Review Task 4 (spec + quality)" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Everything checks out. Now let me verify the diff exactly matches the brief's specified code (it does, verified above) and double check nothing else in the diff is off — e.g., trailing blank lines, placement relative to `score`/`generate`.

The diff shows `contacts` inserted immediately after `score` and before `generate`, matching the brief's instruction ("add after the `score` command"). The test is added right after `test_score_command_default_method`, consistent with file organization. Everything is verified.

### Spec Compliance
- ✅ Spec compliant: `contacts` command added to `src/protlms/cli.py:192-212`, placed immediately after `score` and before `generate`, matching the brief's specified code verbatim. It reuses `_ModelArg`, `_FastaArg`, `_OutputOpt`, `_GpuOpt`, `_BatchOpt`, `_NoPullOpt` module-level annotations. `--method` defaults to `"categorical-jacobian"` and is passed through to `model_obj.contacts(...)`. Errors are wrapped with `except ProtlmsError as exc: _fail(exc)`, matching the sibling commands' pattern exactly (`src/protlms/cli.py:277-280`). The new test `test_contacts_command_invokes_model` (`tests/test_cli.py:236-241`) monkeypatches `protlms.cli.load` and asserts `FakeModel.last_call["contacts_method"] == "categorical-jacobian"`, confirming the method flows through to `Model.contacts`. `FakeModel.contacts` signature matches `Model.contacts`'s real keyword args (`method`, `output_dir`, `use_gpu`, `batch_size`), and `ContactsResult` is correctly imported and constructed with `result`, `output_dir`, `method` fields, matching the real dataclass at `src/protlms/models.py:117-124`.
- ⚠️ Cannot verify from diff: actual `ruff`/`ty check` output isn't in the diff (report claims all pass) — not independently re-run per review instructions, taking the implementer's claim at face value since no contradicting evidence exists in the diff.

### Strengths
- Command signature, body, and docstring match the task brief's prescribed code exactly, with correct placement (`src/protlms/cli.py:192-212`) between `score` and `generate`.
- Test setup (`FakeModel.contacts`, `ContactsResult` import, test function) mirrors the sibling `score` test pattern closely, including use of `last_call` dict for behavioral assertion rather than a mock-call-count check — this asserts real argument propagation, not just "was called."
- Return-value construction in `FakeModel.contacts` matches the real `ContactsResult` dataclass shape (`result`, `output_dir`, `method`), so the test would catch a signature drift in `ContactsResult`.
- Console summary message (`"map(s) written to ... method=..."`) is worded consistently with `score`'s `"variant(s) written to ... method=..."`, keeping CLI output style uniform.

### Issues

#### Minor (Nice to Have)
- `src/protlms/cli.py:192-212` vs `src/protlms/cli.py:162-181` (`score`): `contacts` supports `--no-pull` via `_NoPullOpt` while the sibling `score` command does not (`score` calls bare `load(model)`), so the two "most similar" commands (both single-FASTA/CSV, both take `--method`) are inconsistent with each other on this flag. This is an artifact of the task brief itself instructing `allow_pull=False if no_pull else None` (matching `embed`/`likelihood` instead of `score`) rather than an implementer mistake — flagging for awareness only, since fixing it would mean deviating from the explicit brief, which is out of scope for this task-scoped review.

### Assessment
**Task quality:** Approved
**Reasoning:** The implementation matches the task brief's prescribed code verbatim — correct placement, full option reuse, correct default method, correct error handling, and a test that genuinely exercises argument propagation rather than just command existence. The one inconsistency noted (`--no-pull` present on `contacts` but absent on `score`) traces directly to the brief's own specification, not to implementer deviation, so there is nothing here that needs fixing within this task's scope.</result>
<usage><subagent_tokens>37614</subagent_tokens><tool_uses>14</tool_uses><duration_ms>46694</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a88d76f0ebefe2b7f</task-id>
<tool-use-id>toolu_01CbqL7XyWqfpoym9BQHuo3F</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-protlms/bf2b7b10-35fa-4759-98a9-8aca3a54d050/tasks/a88d76f0ebefe2b7f.output</output-file>
<status>completed</status>
<summary>Agent "Implement Task 5: eval PDB parsing" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Status: DONE
Commits: fd5897c "eval: PDB parsing + true contact map (add biopython dep)"
Tests: 3/3 new tests pass (RED→GREEN confirmed); full suite 214 passed, 29 skipped (Docker-gated, pre-existing).
Concerns: Brief's verbatim code failed `ruff check` on 2 rules (TC003, SIM401); fixed both while preserving behavior — moved `Path` import into `TYPE_CHECKING` (matches `runner.py` convention), and rewrote the CB/CA ternary as explicit if/elif/else with a narrow justified `# noqa: SIM401` (ruff's suggested `.get()` fix would `AttributeError` — biopython's `Residue` has no `.get`). Also note: task-5-report.md previously held stale content from a different "Task 5" (E1 registry entries); overwritten with this task's report.
Report: `/home/briney/git/protlms/.superpowers/sdd/task-5-report.md`</result>
<usage><subagent_tokens>59434</subagent_tokens><tool_uses>48</tool_uses><duration_ms>264460</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a2578e624efbac319</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-protlms/bf2b7b10-35fa-4759-98a9-8aca3a54d050/tasks/a2578e624efbac319.output</output-file>
<status>completed</status>
<summary>Agent "Review Task 5 (spec + quality)" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Confirmed: `long_range_precision_at_l` does not appear anywhere in the diff — correctly deferred. `LONG_RANGE_SEP` is defined but unused within this module (expected, since it's a constant for the later metric task; it's re-exported so ruff won't flag it as unused-in-module since it's a module-level public constant, and `F401`/`RUF` unused-var doesn't apply to module-level assignments).

I now have everything needed to write the review.

### Spec Compliance
- ✅ Spec compliant

All required elements verified directly against the diff and cross-checked against real biopython behavior on the actual `T1024.pdb` fixture:
- Constants exact: `CONTACT_THRESHOLD_ANGSTROM = 8.0` (contacts.py:19), `LONG_RANGE_SEP = 24` (contacts.py:21).
- `PdbChain(sequence: str, resnums: np.ndarray, cb_coords: np.ndarray)` — frozen dataclass, correct shapes documented (contacts.py:46-62).
- `parse_pdb`: uses first model (`next(iter(structure))`), first chain when `chain is None` (`next(iter(model))`) (contacts.py:81-82); filters HETATM/water via `residue.id[0] != " "` and non-standard residues via `is_aa(residue, standard=True)` (contacts.py:87) — verified the fixture has 0 HETATM records, so this path isn't data-exercised but the logic is standard/correct; Cβ-with-Cα-for-Gly fallback verified empirically (`GLY 16` in the fixture: no CB, has CA, `is_aa` true) (contacts.py:93-99); raises `ValueError` when `seq_chars` is empty (contacts.py:105-106).
- `true_contact_map`: symmetric (distance matrix is inherently symmetric), bool dtype (`dist &lt; threshold`), diagonal forced to `False` via `np.fill_diagonal` (contacts.py:120-127); strict `&lt;` matches spec's verbatim "&lt; 8.0 Å".
- `src/protlms/eval/__init__.py` contains only imports and `__all__` — no logic, matches brief verbatim.
- `long_range_precision_at_l` — grepped the entire diff, zero occurrences. Correctly deferred.
- Test file (`tests/test_eval_contacts.py`) matches the brief's verbatim 3 tests exactly, asserting real behavior against the real PDB (sequence/shape/monotonicity/resnum ordering) plus a synthetic 3-point threshold case.
- `pyproject.toml` diff adds exactly `"biopython&gt;=1.83"` under `dependencies`, with the specified comment, in the correct location.
- `tests/data/casp14/T1024.pdb` is a real CASP14 structure (391 ATOM-derived residues, confirmed 20 standard-residue types only, 0 HETATM lines, chain id `' '` — single blank-ID chain, exercising the default `chain=None` path as the report claims).

No items require an unverifiable ⚠️ — everything needed was checkable from the diff plus direct execution against the actual fixture file.

### Strengths
- The two ruff-driven deviations are both correctly reasoned and verified empirically in this review: `Bio.PDB.Residue.Residue` genuinely has no `.get()` (confirmed via `AttributeError` at runtime), so accepting ruff's SIM401 auto-fix verbatim would have broken the code — the implementer caught a real defect in the brief's literal suggestion rather than blindly applying it.
- `Path` is only used as a type annotation (`pdb: Path`) and immediately coerced via `str(pdb)`; combined with `from __future__ import annotations`, moving the import under `TYPE_CHECKING` is behaviorally inert and matches the existing precedent in `src/protlms/runner.py:19-20` exactly.
- Test file is untouched from the brief's spec — no scope creep, no gold-plating.
- Constants, docstrings (Google-style), type hints, and module layout all conform to project conventions.
- Report is honest and specific about deviations, includes real verification transcripts (RED/GREEN, ruff/ty output), and explicitly confirms `long_range_precision_at_l` was not added.

### Issues

#### Critical (Must Fix)
None.

#### Important (Should Fix)
None.

#### Minor (Nice to Have)
- `src/protlms/eval/contacts.py:95` — the `# noqa: SIM401` is placed on the `elif "CA" in residue:` line, but the actual ruff SIM401 pattern match is more naturally associated with the whole if/elif/else construct; a reader skimming just that line without the preceding comment might not immediately see why `noqa` sits there rather than on the `if` line. Not incorrect (the justification comment right above it makes intent clear), just slightly less obvious placement. No functional impact.
- The HETATM/water-filtering branch (`residue.id[0] != " "`) is exercised by zero lines in `T1024.pdb` (confirmed 0 `HETATM` records), so this filter is logic-verified by inspection but not test-verified against real data. This is inherited from the brief's fixture choice, not an implementer error, and is appropriately out of scope to fix here (brief specifies copying this exact file).

### Assessment
**Task quality:** Approved
**Reasoning:** Implementation matches the brief's interfaces and metric definitions exactly (constants, shapes, dtypes, filtering behavior), `long_range_precision_at_l` is correctly absent, and both deviations from the brief's literal code are verified here to be genuine bug-avoidance (confirmed `Residue.get()` raises `AttributeError`) and behavior-preserving (confirmed `Path` never constructed at runtime), not shortcuts.</result>
<usage><subagent_tokens>56141</subagent_tokens><tool_uses>10</tool_uses><duration_ms>53904</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>aa9a121f77a5d8ce1</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-protlms/bf2b7b10-35fa-4759-98a9-8aca3a54d050/tasks/aa9a121f77a5d8ce1.output</output-file>
<status>completed</status>
<summary>Agent "Implement Task 6: precision@L metric" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Perfect! Now let me create a final concise summary:

---

## Status: COMPLETE

**Commit:** `bfa4230` — eval: add long-range precision@L metric

**Test Summary:** All 5 tests in `test_eval_contacts.py` pass (3 existing + 2 new precision@L tests). Both TDD test cases are hand-checkable and passing:
- `test_long_range_precision_at_l_known_case`: 6-residue system, sep=3, precision calculation verified
- `test_long_range_precision_at_l_no_eligible_pairs_is_nan`: NaN return for empty pair set confirmed

**Files Changed:**
- `src/protlms/eval/contacts.py` — added `long_range_precision_at_l(pred, true, resnums, *, sep=LONG_RANGE_SEP, top=None) -&gt; float`
- `src/protlms/eval/__init__.py` — exported function + added to `__all__`
- `tests/test_eval_contacts.py` — added `import pytest` and 2 new test functions

**Code Quality:** ruff check ✓ | ruff format ✓ | type check ✓ | No issues

**Concerns:** None

**Report:** `/home/briney/git/protlms/.superpowers/sdd/task-6-report.md`</result>
<usage><subagent_tokens>38163</subagent_tokens><tool_uses>20</tool_uses><duration_ms>102763</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>ae64d28ba1d50bef4</task-id>
<tool-use-id>toolu_0179q2V7Z9Gbhgko9uNkSLgA</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-protlms/bf2b7b10-35fa-4759-98a9-8aca3a54d050/tasks/ae64d28ba1d50bef4.output</output-file>
<status>completed</status>
<summary>Agent "Review Task 6 (spec + quality)" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Everything matches the brief exactly — implementation is a verbatim match to the brief's specified code. Let me double check the second test (no eligible pairs → nan) and confirm shape-mismatch behavior isn't tested but is present in code.

Test 2: n=3, sep=24, resnums=[0,1,2]. Max |i-j| for n=3 is 2 (0 vs 2), which is &lt; 24, so no pairs are eligible → `i.size == 0` → returns `nan`. Correct.

One thing to note: there's no test for the `ValueError` on shape mismatch, and no test for `top` exceeding eligible count in a way that changes clipping behavior (though the default-top test does exercise clipping since `top=None → k=6` equals eligible count exactly — it doesn't test `top &gt; eligible count` with a `top` value larger than N). These are coverage gaps, not correctness bugs, given the brief only mandated the two specified tests.

Let me verify the branch's base commit reference (`Task 5`) actually exports `true_contact_map`/`LONG_RANGE_SEP` as claimed, to confirm the dependency claim in the brief is intact (not part of this diff, but worth a quick sanity check via the diff context already shown — confirmed above, `LONG_RANGE_SEP` and `true_contact_map` already existed pre-diff).

### Spec Compliance
- ✅ Spec compliant
  - (a) Uses `np.triu_indices(n, k=1)` — single-count upper-triangle, confirmed at `src/protlms/eval/contacts.py:87` (post-diff line numbers; diff shows it added after line 119 in the pre-diff file, effectively landing near line 130s in the new file).
  - (b) Eligibility filter `np.abs(resnums[i] - resnums[j]) &gt;= sep` — correct, non-strict `&gt;=` as specified.
  - (c) Ranks by `pred` descending via `np.argsort(-pred[i, j], kind="stable")` — correct direction and stable tie-breaking.
  - (d) `top=None` ⇒ `k = i.size` (i.e., N eligible pairs after filtering — note: per the brief this is "L = N" where N is number of resolved residues, but the code uses "N = number of eligible pairs" as the default k; this coincidentally equals the residue count only when eligible-pair count happens to match N, as in the test's crafted 6/6 case — see note below). `top` otherwise clipped via `min(int(top), i.size)`. Confirmed correct clipping semantics.
  - (e) Returns `float("nan")` when `i.size == 0` (zero eligible pairs). Correct.
  - (f) Raises `ValueError` with a descriptive message on any shape mismatch among `pred`, `true`, `resnums`. Correct.
  - (g) Exported from `src/protlms/eval/__init__.py:9` (import) and `:18` (`__all__`). Correct.
  - **Hand-checked both known-answer tests myself, independent of the report:**
    - `top=2` case: eligible pairs ranked by pred descending → (0,3)=0.9 [true], (0,4)=0.8 [false], remaining ties at 0. Top-2 = {true, false} → precision = 1/2 = **0.5**. Matches assertion.
    - Default (`top=None`) case: eligible pair count = 6 (matches n=6 coincidentally), so `k=6`, selecting all eligible pairs. True contacts among the 6 eligible pairs: (0,3) and (1,5) → 2 true → **2/6**. Matches assertion.
  - Both asserted values are genuinely correct given the crafted inputs.

- ⚠️ Cannot verify from diff: whether `top=None → N` in the brief's prose ("L = N, number of resolved residues") is faithfully distinguished from "N = number of eligible pairs" in general (non-coincidental) cases — see Minor note below. This is a documentation/semantic nuance, not a test failure, since the brief's own Step 3 reference implementation (which the implementer copied verbatim) defines `k = i.size if top is None else ...`, i.e., eligible-pair count, not residue count. The implementer followed the brief's literal code exactly, so this is inherited from the brief itself, not an implementer deviation.

### Strengths
- Implementation is a byte-for-byte match to the brief's Step 3 reference code — no deviation, no scope creep.
- Docstring is Google-style, complete with Args/Returns, and accurately describes behavior.
- Vectorized numpy usage is clean and idiomatic (`triu_indices`, boolean masking, `argsort` with explicit `kind="stable"`).
- Shape validation covers all three inputs and produces an informative error message.
- Both hand-checkable tests are genuinely hand-derivable and I independently verified the arithmetic — not just trusting the implementer's comments.
- `__init__.py` export follows existing alphabetization/grouping conventions in the file.

### Issues

#### Critical (Must Fix)
None.

#### Important (Should Fix)
None.

#### Minor (Nice to Have)
- `src/protlms/eval/contacts.py` (`long_range_precision_at_l`, the `k = i.size if top is None else ...` line) — the brief's prose says `top=None ⇒ L = N` where N is "number of resolved residues" (i.e., `pred.shape[0]`), but the code (and brief's own reference implementation) sets the default to the *eligible pair count*, not the residue count. In this task's test, those two happen to coincide (6 residues, 6 eligible pairs), masking the discrepancy. In general N residues will have far more than N eligible long-range pairs (e.g., 100 residues, sep=24 → many more than 100 eligible pairs), so "default top" behaves as "score all eligible pairs" rather than "top-N pairs" — this could matter if a caller assumes default behavior mirrors literal CASP-style precision@L (usually L = N/something, not "all pairs"). Since this is inherited verbatim from the brief's spec/reference code (not an implementer error), it's not a fault of this implementation, but worth flagging so a future consumer isn't surprised that the "default" isn't literally top-N-by-residue-count. No fix required for this task; flag for spec owners if it matters downstream.
- `tests/test_eval_contacts.py` — no test exercises the `ValueError` shape-mismatch path or a `top` value that both exceeds the eligible-pair count and differs from `N`residues (to more clearly distinguish "eligible pair count" vs "residue count" semantics). Not required by the brief's Step 1, but would harden coverage.

### Assessment
**Task quality:** Approved
**Reasoning:** The implementation is a verbatim, correct realization of the brief's specification and reference code; both hand-checkable test values were independently re-derived and confirmed correct, all metric semantics (single-count triu, `&gt;=` separation, descending stable rank, nan-on-empty, ValueError on shape mismatch, export) are satisfied, and no critical or important defects were found.</result>
<usage><subagent_tokens>34999</subagent_tokens><tool_uses>5</tool_uses><duration_ms>38521</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a93f1709a870b96d1</task-id>
<tool-use-id>toolu_01SaRSL1jeirq3TcSScRbiEi</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-protlms/bf2b7b10-35fa-4759-98a9-8aca3a54d050/tasks/a93f1709a870b96d1.output</output-file>
<status>completed</status>
<summary>Agent "Fix Task 6 precision@L default" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Status: DONE; commit 99072cb "fix(eval): precision@L default top is L=N residues, not eligible-pair count"; tests: `test_long_range_precision_at_l_default_top_is_residue_count_not_pair_count` (RED 0.4 → GREEN 0.5) + `test_long_range_precision_at_l_shape_mismatch_raises` (new) + 2 pre-existing precision@L tests, all 7 tests in `tests/test_eval_contacts.py` pass; report: `/home/briney/git/protlms/.superpowers/sdd/task-6-report.md`</result>
<usage><subagent_tokens>43207</subagent_tokens><tool_uses>12</tool_uses><duration_ms>69257</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>ac73392a3b54fb592</task-id>
<tool-use-id>toolu_015mxKQtNGVKs7DNSGrbFeSe</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-protlms/bf2b7b10-35fa-4759-98a9-8aca3a54d050/tasks/ac73392a3b54fb592.output</output-file>
<status>completed</status>
<summary>Agent "Re-review Task 6 after fix" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Now let me verify the fix computation and hand-trace the new regression test.

**Step 1: `k` computation.** In `contacts.py`:
```python
k = n if top is None else int(top)
k = min(k, i.size)
```
`n = pred.shape[0]` (residue count). When `top is None`, `k = n`; otherwise `k = int(top)`. Then clipped to `i.size` (eligible-pair count) in both cases. This matches the spec exactly.

**Step 2: Hand-trace `test_long_range_precision_at_l_default_top_is_residue_count_not_pair_count`.**

n=6, resnums=0..5, sep=1 → all `|i-j|&gt;=1` pairs eligible → all `C(6,2)=15` upper-triangle pairs eligible. `i.size = 15`.

pred nonzero entries: (0,1)=0.9, (0,2)=0.8, (0,3)=0.7, (0,4)=0.6, (0,5)=0.5, (1,2)=0.4. All other 9 pairs = 0.0.

true entries: (0,1), (0,3), (0,5), (1,3), (2,4), (3,5) = True (6 total).

Default: `top=None` → `k = n = 6`, clipped to `min(6, 15) = 6`.

Rank by descending pred (stable for ties, and among the zero-score pairs order preserves `np.triu_indices` order i.e. row-major (i,j) with i&lt;j):

1. (0,1)=0.9 → true
2. (0,2)=0.8 → false
3. (0,3)=0.7 → true
4. (0,4)=0.6 → false
5. (0,5)=0.5 → true
6. (1,2)=0.4 → false

Top-6 = {(0,1)T, (0,2)F, (0,3)T, (0,4)F, (0,5)T, (1,2)F} → 3 true / 6 = **0.5**. Matches assertion `== 0.5`. ✓

Now check what scoring all 15 eligible pairs would give (i.e., the old buggy `k = i.size = 15` behavior): all 6 true pairs are somewhere among the 15 (regardless of rank, since k=15 includes everything), so precision = 6/15 ≈ 0.4. This confirms the test **would fail under the old code** (0.4 ≠ 0.5), and the report's own RED-phase log (`assert 0.4 == 0.5`) confirms this empirically. The test genuinely distinguishes the two behaviors. ✓

**Step 3: Shape-mismatch test.**
```python
long_range_precision_at_l(np.zeros((4, 4)), np.zeros((4, 4), dtype=bool), np.arange(3))
```
Here `pred.shape=(4,4)` so `n=4`; `true.shape=(4,4)` matches `(n,n)`; but `resnums.shape=(3,)` ≠ `(n,)=(4,)` → triggers the `ValueError` branch as coded. Wrapped in `pytest.raises(ValueError)`. Correctly exercises the intended path. ✓

**Step 4: Pre-existing `test_long_range_precision_at_l_known_case` default-top assertion.**

n=6, resnums=0..5, sep=3 → eligible pairs (|i-j|&gt;=3): (0,3),(0,4),(0,5),(1,4),(1,5),(2,5) → `i.size = 6`. Default: `k = n = 6`, clipped to `min(6,6)=6` → unchanged from before the fix. True contacts among these 6: (0,3) and (1,5) → 2/6. Matches `pytest.approx(2/6)`. Confirmed unaffected by the fix, as claimed. ✓

**Step 5: Spec compliance overall.**

- `&gt;=` separation: `np.abs(resnums[i]-resnums[j]) &gt;= sep` ✓
- Descending stable rank: `np.argsort(-pred[i,j], kind="stable")` ✓
- `nan` on zero eligible pairs: `if i.size == 0: return float("nan")` ✓ (checked before computing `k`, so no div-by-zero risk even in edge cases)
- `ValueError` on shape mismatch: present, checks all three arrays against `(n,n)`/`(n,)` ✓
- Default `top=None` → `top = N` (residue count), clipped to eligible-pair count: fixed correctly ✓
- Explicit `top` → `int(top)`, clipped to eligible-pair count: ✓
- Exported from `eval/__init__.py`: added to both import and `__all__` ✓

One thing worth noting for completeness: if a caller passes `top=0` or a negative `top`, `k = int(top)` then `min(k, i.size)` could yield `k&lt;=0`, causing a `ZeroDivisionError` or nonsensical negative-count division at the final `/ float(k)` line. This is out of scope per the brief (which only specifies `top: int | None`, no explicit bounds-validation requirement), and isn't part of the bug being fixed, so it's a pre-existing minor gap rather than a regression.

### Spec Compliance
- ✅ Spec compliant

### Strengths
- The `k` fix is minimal and precise: only the `k = ...` line was touched, with a clean two-line rewrite (`k = n if top is None else int(top)`, then `k = min(k, i.size)`) that correctly separates "compute intended L" from "clip to eligible pairs," exactly matching the brief's binding semantics.
- The new regression test (`test_long_range_precision_at_l_default_top_is_residue_count_not_pair_count`) is well constructed: sep=1 forces all 15 pairs eligible while N=6, cleanly separating the two candidate behaviors (0.5 vs 0.4) that were previously indistinguishable in the n==eligible-count coincidence of the original test.
- RED-phase evidence in the report (`assert 0.4 == 0.5`) independently corroborates that the test fails on the pre-fix code, not just in theory.
- The shape-mismatch test and its `ValueError` path are correctly wired and add real coverage that was previously untested.
- Pre-existing tests were re-verified by hand to still hold under the new code, and the reasoning (n == eligible count in that fixture) is correct.
- Export change in `__init__.py` is complete and consistent (import + `__all__`).

### Issues

#### Critical (Must Fix)
None.

#### Important (Should Fix)
None.

#### Minor (Nice to Have)
- `src/protlms/eval/contacts.py:94-97` — no validation that `top` is positive; `top=0` or a negative `top` propagates to `k = min(k, i.size)` and then `float(true[...].sum()) / float(k)`, which either divides by zero or by a negative number instead of raising a clear error. Not part of this bug's scope and not required by the brief, but worth a follow-up guard (e.g., raise `ValueError` for `top &lt;= 0`) if the function is ever exposed to untrusted/CLI input.

### Assessment
**Task quality:** Approved
**Reasoning:** The `k` computation now correctly implements `top=None ⇒ L=N` clipped to the eligible-pair count, matching the brief's binding semantics exactly; the new regression and shape-mismatch tests were hand-traced and confirmed to genuinely exercise the fixed behavior (including independent confirmation that the old code would fail them), and the two pre-existing tests still pass under the corrected logic for the reasons stated.</result>
<usage><subagent_tokens>36024</subagent_tokens><tool_uses>3</tool_uses><duration_ms>28652</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a3278d2dbc1f1ede8</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-protlms/bf2b7b10-35fa-4759-98a9-8aca3a54d050/tasks/a3278d2dbc1f1ede8.output</output-file>
<status>completed</status>
<summary>Agent "Implement Task 7: eval runner" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Status: DONE
Commit: edf98fd — "eval: add CASP14 contacts runner + CSV output"
Tests: `tests/test_eval_runner.py` 3/3 passed; full suite 221 passed, 29 skipped (Docker-only); ruff check/format and `ty check` clean.

Concern: the brief's Step 3 reference code, run verbatim against its own Step 1 test, fails — `max_length` filtering out the only target left `records=[]`, which triggered `InvalidRequestError` instead of the test's expected `[]`. Fixed by tracking whether any `.pdb` parsed successfully (`parsed_any`) separately from the post-`max_length` record list, so `InvalidRequestError` now fires only when nothing in the directory parses at all, and `max_length` fully excluding targets returns `[]`. Docstring updated to match. Full details, RED/GREEN evidence, and self-review in the report.

Report: `/home/briney/git/protlms/.superpowers/sdd/task-7-report.md`</result>
<usage><subagent_tokens>72787</subagent_tokens><tool_uses>29</tool_uses><duration_ms>161558</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a121dd22cc4312a59</task-id>
<tool-use-id>toolu_01MC38ctc3cdf2geCzQuLHr8</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-protlms/bf2b7b10-35fa-4759-98a9-8aca3a54d050/tasks/a121dd22cc4312a59.output</output-file>
<status>completed</status>
<summary>Agent "Review Task 7 (spec + quality)" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>No pandas anywhere, `csv` stdlib module used correctly, numpy used only for the metric computation. `Model` is imported under `TYPE_CHECKING` only (line 41-42 in the diff), confirmed by `ty check` passing and the successful runtime test with a duck-typed `FakeModel` that never imports `protlms.models`.

I now have everything needed to write the verdict.

### Spec Compliance
- ✅ Spec compliant
- ⚠️ Cannot verify from diff: whether a future CLI-wiring task will need `TargetResult`/`evaluate_contacts` re-exported from `protlms.eval.__init__` — the implementer flagged this as an open question but it's out of scope for this task per the brief, and `__init__.py` is unmodified in the diff (consistent with in-scope behavior).

**Verified directly (diff read + targeted runtime checks, per instructions not to re-run the given suite but to check specific behaviors):**
- Single `model.contacts` call for all targets: confirmed structurally (`runner.py` lines 124-127 of diff, one call inside the `with tempfile.TemporaryDirectory` block, outside any per-target loop) and empirically — instrumented a counting fake model with 2 targets, `call_count == 1`.
- Per-target guards: no-map-for-id → warn+skip (`runner.py:133-135`); shape != `(n,n)` → warn+skip (`runner.py:136-143`). Both verified empirically with a model that omits one target's map and returns a wrong-shape map for another — only the valid target survived.
- `InvalidRequestError` vs `[]` split: `parsed_any` (true if ≥1 `.pdb` parsed) gates the exception (`runner.py:119-120`); `records` (post-`max_length`-filter) gates the early `[]` return (`runner.py:121-122`), placed *before* the FASTA-staging/`model.contacts` block. Verified empirically both branches: an empty directory raises `InvalidRequestError`; a directory with one parseable-but-too-long target returns `[]` with a `BoomModel` that asserts if called — confirmed zero calls.
- CSV columns/format: header exactly `target_id,length,n_long_range_true,precision_at_l`; `precision_at_l` formatted `.6f` (`0.5` → `"0.500000"`, satisfying the test's `startswith`). `write_results_csv` uses stdlib `csv.writer`.
- `mean_precision`: filters `math.isnan` before `np.mean`; returns `nan` for all-nan/empty input — matches Task 6 convention.
- No pandas anywhere in the diff; only `numpy` + stdlib `csv`.
- `Model` imported only under `TYPE_CHECKING` (`runner.py:41-42`); confirmed the test's `FakeModel` is duck-typed with zero import of `protlms.models`, and `ty check src/` passes clean.
- The 3 tests assert real behavior: test 1 parses a real PDB fixture (`tests/data/casp14/T1024.pdb`), drives a duck-typed `FakeModel`, and checks real computed fields (length, precision bounds, count &gt; 0) — not mocked-through assertions. Test 2 exercises the brief-contradiction fix directly. Test 3 checks CSV byte format and `mean_precision` arithmetic against hand-computed values (0.5, 0.25 → mean 0.375).
- The brief's internal contradiction and the implementer's resolution (`parsed_any` vs `records`) is sound: it preserves "genuinely empty/unusable directory" as an error while treating "valid directory, but everything filtered by a client-side option" as a legitimate empty-result case — arguably the more defensible API, and it's exercised by the brief's own test.

### Strengths
- The `parsed_any`/`records` split is a clean, minimal fix to a real spec contradiction, is well-documented in both the docstring (`Returns`/`Raises` sections, `runner.py:88-94`) and the report, and doesn't overreach past what's needed to satisfy both the "no usable targets" error case and the "all filtered by option" empty-list case.
- Correct id-based join between parsed `chains` and returned `maps` (`runner.py:130-131`) rather than assuming positional/order correspondence — robust against a container returning maps in different order or a subset.
- Parse failures are isolated per-file (`try/except (ValueError, KeyError)` at `runner.py:102-106`) so one malformed PDB doesn't abort the whole batch; consistent with the per-target-guard philosophy used elsewhere in the function.
- FASTA staging correctly uses a `tempfile.TemporaryDirectory` scoped only around the `model.contacts` call, so temp files are cleaned up immediately after the container/fake call returns, before per-target scoring proceeds — no lingering temp state.
- Verified empirically (not just by reading the diff) that `model.contacts` is never invoked on the empty-records path — a `BoomModel` that raises if called was passed through the `max_length`-excludes-everything scenario and never triggered.

### Issues

#### Critical (Must Fix)
None.

#### Important (Should Fix)
None.

#### Minor (Nice to Have)
- `src/protlms/eval/runner.py:104` — catches `(ValueError, KeyError)` from `parse_pdb`, but `parse_pdb`'s own docstring (`contacts.py`) only documents raising `ValueError`. Catching `KeyError` too is defensive (e.g., BioPython chain-lookup misses), but since it's not part of `parse_pdb`'s documented contract, a comment noting why `KeyError` is included would help future readers avoid assuming it's dead code and deleting it.
- `src/protlms/eval/runner.py:130` — iterates `chains.items()` to build `results`, relying on Python dict insertion-order preservation (guaranteed since 3.7, so not a correctness bug) to keep file order; an inline comment noting this ordering dependency would save a future reader from wondering whether id-based iteration order is guaranteed.
- Report flags but leaves open whether `protlms.eval.__init__` should re-export the new symbols for a hypothetical future CLI task — reasonable to defer since the brief doesn't request it and no current consumer needs it.

### Assessment
**Task quality:** Approved

**Reasoning:** All brief requirements are met and independently verified at runtime (single batched `model.contacts` call, both per-target guards, CSV format, nan-safe mean, TYPE_CHECKING-only `Model` import, no pandas); the implementer's resolution of the brief's internal test/reference-code contradiction is sound, minimal, and empirically confirmed not to call `model.contacts` on the empty-records path. Only minor documentation nits remain.</result>
<usage><subagent_tokens>46338</subagent_tokens><tool_uses>17</tool_uses><duration_ms>90456</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>aec1a0aaec0c6e976</task-id>
<tool-use-id>toolu_01YM6gyBDkjhLRC2i1gkjBMN</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-protlms/bf2b7b10-35fa-4759-98a9-8aca3a54d050/tasks/aec1a0aaec0c6e976.output</output-file>
<status>completed</status>
<summary>Agent "Implement Task 8: CLI eval contacts" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>---

## Status: COMPLETE

**Commit:** `2b2ab21` — `cli: add protlms eval contacts benchmark command`

**Tests:** 19/19 passing (1 new test: `test_eval_contacts_command`)

**No concerns.** All checks passed: linting (ruff), formatting, type checking, and full test suite. Implementation follows brief exactly with TDD approach: test written first (RED), then implementation (GREEN), then verification.

**Report:** `/home/briney/git/protlms/.superpowers/sdd/task-8-report.md`</result>
<usage><subagent_tokens>36239</subagent_tokens><tool_uses>19</tool_uses><duration_ms>78014</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a1890868eff811f03</task-id>
<tool-use-id>toolu_01V7Giv11m7HxfoeHoPKWj8u</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-protlms/bf2b7b10-35fa-4759-98a9-8aca3a54d050/tasks/a1890868eff811f03.output</output-file>
<status>completed</status>
<summary>Agent "Review Task 8 (spec + quality)" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Everything checks out. `FakeModel.manifest.name` = `"esm2_t6_8M"`, which is used in the table title but not asserted in the test — fine, not required by spec. Now let's verify all pieces per the checklist.

### Spec Compliance
- ✅ Spec compliant

All required elements verified against `src/protlms/cli.py` (diff lines 27-33, 54-109) and `tests/test_cli.py` (diff lines 139-155):

- `eval_app = typer.Typer(...)` registered via `app.add_typer(eval_app, name="eval")` — matches brief exactly (cli.py:29-33).
- `@eval_app.command("contacts")` with signature matching the brief: `model: _ModelArg` (positional), `--pdb-dir` (required, `exists=True, file_okay=False`), `--out` (optional `Path`), `--sep` (default `24`), `--top`, `--max-length`, `--gpu` (`_GpuOpt`), `--batch-size` (`_BatchOpt`), `--no-pull` (`_NoPullOpt`) — all present and correctly typed (cli.py:55-77).
- `evaluate_contacts`, `mean_precision`, `write_results_csv` imported **inside** the function body (cli.py:79: `from protlms.eval.runner import evaluate_contacts, mean_precision, write_results_csv`), which is required for the test's `protlms.eval.runner.evaluate_contacts` monkeypatch to take effect (module-level import would bind the original at import time).
- `load(model, allow_pull=False if no_pull else None)` — matches brief precisely (cli.py:82).
- `evaluate_contacts(model_obj, pdb_dir, sep=sep, top=top, use_gpu=gpu, batch_size=batch_size, max_length=max_length)` — all kwargs passed through correctly, matching `evaluate_contacts`'s real signature in `src/protlms/eval/runner.py:47-56` (`sep`, `top`, `use_gpu`, `batch_size`, `max_length` all keyword-only there too).
- Rich `Table` built with columns `target`, `L`, `LR contacts`, `P@L`, rows populated from `TargetResult` fields in the same field order the real dataclass defines (`target_id, length, n_long_range_true, precision_at_l` — verified against `runner.py:31-37`).
- Mean-precision line via `mean_precision(results)`, formatted and printed.
- `--out` gates `write_results_csv(results, out)` — only called `if out is not None`.
- `except ProtlmsError as exc: _fail(exc)` wraps the whole body — matches brief and the pattern used by every other command (e.g. `generate`, `embed`) in the same file.
- Test (`tests/test_cli.py:139-155`) monkeypatches both `protlms.cli.load` and `protlms.eval.runner.evaluate_contacts`, invokes `eval contacts esm2-8m --pdb-dir ... --out ...`, and asserts exit code 0, `"T1024"` in stdout, and CSV file exists — matches the brief's Step 1 test verbatim.

No ⚠️ items — all claims are directly verifiable from the diff plus the pre-existing (unchanged) `protlms/eval/runner.py` and `tests/test_cli.py` fixtures, which were read to confirm signature/field compatibility.

### Strengths
- Faithful, near-verbatim implementation of the brief's prescribed code — no deviation in option names, defaults, or wiring.
- Correct use of the late-import pattern specifically to support monkeypatching, with a clear understanding of *why* it's needed (also explained accurately in the report).
- Reuses existing shared option definitions (`_GpuOpt`, `_BatchOpt`, `_NoPullOpt`, `_ModelArg`) rather than redefining them, consistent with sibling commands like `embed`/`generate`.
- Error handling (`except ProtlmsError as exc: _fail(exc)`) is consistent with every other command in the file.
- Test asserts genuinely meaningful behavior: real exit code, real content in rendered table output, and real CSV side-effect — not a vacuous "doesn't crash" check.

### Issues

#### Critical (Must Fix)
None.

#### Important (Should Fix)
None.

#### Minor (Nice to Have)
- `src/protlms/cli.py:98` — `table.add_row` line was reflowed to multi-line form by the implementer (per report, "Fixed long line... by splitting across 2 lines") rather than matching the brief's single-line form. This is a cosmetic deviation only, required to satisfy the 100-char line-length rule; behavior is identical. Not a real issue, noted only for completeness.
- `tests/test_cli.py:139-155` — The test doesn't assert on the mean-precision line or verify CSV contents (e.g., that the row for `T1024` was actually written correctly), only that the file exists. This matches the brief's prescribed test exactly, so it's not a deviation, but a slightly stronger test could assert `out_csv.read_text()` contains `T1024` to close the loop on the `write_results_csv` passthrough. Coverage nice-to-have, not a defect.

### Assessment
**Task quality:** Approved
**Reasoning:** The implementation matches the brief's prescribed code and interfaces exactly — sub-app registration, command signature/defaults, in-function imports for monkeypatch compatibility, correct kwarg passthrough to `evaluate_contacts`, table/CSV/mean-precision rendering, and error wrapping all verified directly against the diff and the pre-existing `eval/runner.py` contract. No functional, spec, or quality issues found.</result>
<usage><subagent_tokens>38528</subagent_tokens><tool_uses>10</tool_uses><duration_ms>40692</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a088713b0bf4f8da0</task-id>
<tool-use-id>toolu_01WpNnDnuG5yGqLdB366dHv3</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-protlms/bf2b7b10-35fa-4759-98a9-8aca3a54d050/tasks/a088713b0bf4f8da0.output</output-file>
<status>completed</status>
<summary>Agent "Implement Task 9: generalize esm container" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Report written. This report file itself is untracked/modified relative to the commit but that's expected (task briefs/reports aren't part of the code commit per the brief's instructions).

Status: DONE
commit: `59aacee` "esm: generalize esm2 image into shared esm container (esm1b+esm2)"
tests: 218 passed, 29 deselected (`-m "not slow"`); ruff check/format and `ty check` all clean
concerns: touched `tests/test_integration_chunking.py` (not in brief's file list) because it built from the now-gone `containers/esm2` path and would resolve to the wrong image post-rename — fixed it the same way as `test_integration_esm.py` (pinned with `allow_pull=False`, matching the existing protbert pattern); also updated `build_parser()`'s stale `prog="esm2"` for consistency. Self-review grep confirms all remaining `esm2`/`ESM2_CHECKPOINT`/`protlms-esm2` references live only in self-contained test fixtures or other model families' containers, none of which touch the real registry/container paths.
report: `/home/briney/git/protlms/.superpowers/sdd/task-9-report.md`</result>
<usage><subagent_tokens>95498</subagent_tokens><tool_uses>62</tool_uses><duration_ms>363992</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a34044a417c8b7e2e</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-protlms/bf2b7b10-35fa-4759-98a9-8aca3a54d050/tasks/a34044a417c8b7e2e.output</output-file>
<status>completed</status>
<summary>Agent "Review Task 9 (spec + quality)" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>This confirms all globals are consistently replaced (no stray `DEFAULT_CHECKPOINT` remaining in the entrypoint), `CONTRACT_VERSION = "0.4"` is used everywhere it should be, and both `cmd_score` (line 402) and `_write_capability_result` (line 439) use `MODEL_NAME`. The top-level `CLAUDE.md` still references `containers/esm2/` and `ESM2_CHECKPOINT` — but that's explicitly called out by the implementer as intentionally out of scope (not in the brief's file list), which is reasonable for a task-scoped rename (it's a pre-existing doc staleness, not something this task broke).

I now have everything needed to write the verdict.

### Spec Compliance
- ✅ Spec compliant
- ⚠️ Cannot verify from diff: actual `docker build`/`docker run` behavior (integration tests are Docker-gated and correctly not run); whether `ty check src/` output is accurate (only entrypoint.py, a container script, isn't under `ty check src/` scope anyway, so this is moot).

All required elements verified directly in the diff and via targeted greps against `59aacee`:
- `containers/esm2/` → `containers/esm/` via true git-detected rename (`entrypoint.py` 92% similarity; `Dockerfile`/`README.md` show as add+delete only because their content changed substantially, per the brief's own instructions — this is expected, not a defect).
- Entrypoint globals: `HF_ID`/`MODEL_NAME`/`MODEL_FAMILY` added exactly as specified (`containers/esm/entrypoint.py:27-29`); `resolve_hf_id` and `DEFAULT_CHECKPOINT`/`ESM2_CHECKPOINT` fully removed (confirmed via repo-wide grep — zero occurrences outside other model families and historical docs).
- `load_model` (`hf_id = HF_ID`, line 145), `cmd_prefetch` (`hf_id = HF_ID`, line 300), `build_manifest` (`AutoConfig.from_pretrained(HF_ID)`, `"name": MODEL_NAME`, `"model_family": MODEL_FAMILY`, description templated correctly, lines 156-162), `cmd_score` (line 402) and `_write_capability_result` (line 439) both use `"model_name": MODEL_NAME` — all call sites updated, none missed.
- `CONTRACT_VERSION = "0.4"` (line 24), used consistently in both result-writing call sites.
- `Dockerfile` matches the brief verbatim: three build args exported as env vars, `RUN python /app/entrypoint.py _prefetch` before `ENV HF_HUB_OFFLINE=1` (prefetch→offline ordering preserved).
- `README.md` rewritten with `protlms-esm` image name, new build args, ESM-1b + ESM-2 coverage, added `Family` column and `esm1b_t33_650M` row.
- `models.yaml`: both `esm2-8m`/`esm2-650m` entries updated to `ghcr.io/briney/protlms-esm:&lt;tag&gt;`, `context: containers/esm`, and the three-arg `build.args` block — exact values match the brief.
- `tests/test_esm_entrypoint.py`: path constant updated (`containers/esm/entrypoint.py`), `test_resolve_hf_id` and its parametrize block deleted, all other pure-helper tests kept intact.
- `tests/test_integration_esm.py`: `IMAGE` updated, build fixture uses the three new build args against `containers/esm`, fixture renamed `esm2_image` → `esm_image` with all references updated (including the `model` fixture parameter).
- `tests/test_registry.py`: `test_default_registry_resolves_esm2_8m` image assertion updated to `protlms-esm:t6_8M`; the `_entry()`-based tests using synthetic `containers/esm2`/`ESM2_CHECKPOINT`/`protlms-esm2` strings (lines 73, 80, 84, 98, 100-101) are untouched, correctly, since they construct standalone `ModelEntry` objects unrelated to the packaged registry.

### Strengths
- Clean, minimal diff — every hunk in the entrypoint traces directly to the brief's instructions; no incidental refactoring or scope creep in the core rename.
- Self-review grep table in the report is verifiable and accurate — independently re-ran the same greps and got matching results (all remaining `esm2`/`ESM2_CHECKPOINT`/`protlms-esm2` strings live in synthetic test fixtures or other model families' containers, not on any real code path).
- The `test_integration_chunking.py` fix is correct and necessary: it built from the now-deleted `containers/esm2` and would have collected against a nonexistent directory, silently breaking the (currently skipped, Docker-gated) test. The fix mirrors `test_integration_esm.py` exactly and additionally applies `allow_pull=False`, matching the existing protbert local-pin pattern (commit `ccad1c3`) — a sensible, well-justified addition given the task's stated goal that "embed/likelihood/score behavior must be unchanged" (an unpinned image resolving to a nonexistent GHCR tag would have been a functional break, not just cosmetic).
- The `prog="esm2"` → `prog="esm"` argparse rename is correct and in-scope: it's purely cosmetic CLI metadata inside the same file being generalized, consistent with the rename's intent, and carries no behavioral risk (nothing in the client/tests reads `prog`).
- Docstrings (module-level, README) were coherently rewritten to reflect the new shared-family framing rather than left as stale ESM2-only prose.

### Issues

#### Critical (Must Fix)
None.

#### Important (Should Fix)
None. No real (non-fixture, non-historical-doc) path still references `containers/esm2` or `protlms-esm2`; the registry/manifest/result wiring is fully consistent with the new globals.

#### Minor (Nice to Have)
- `containers/esm/entrypoint.py:8` (module docstring) still says "Exposes the `manifest`, `embed`, `likelihood`, and `score` subcommands" — accurate for this task's scope (score is unchanged), just noting it will need another touch when `contacts` lands in Task 11. Not a defect now.
- Top-level `CLAUDE.md:93-94` and `README.md:24,53` and `docs/CONTRACT.md:327` still reference `containers/esm2/`/`ESM2_CHECKPOINT` post-rename. Correctly out of the brief's file list and flagged by the implementer as intentionally not modified, but worth a follow-up task so the architecture docs don't drift from the actual container layout.

### Assessment
**Task quality:** Approved
**Reasoning:** Every spec requirement is verifiably implemented exactly as specified (globals, manifest/result wiring, Dockerfile, registry, tests), the two out-of-brief changes are correct and low-risk consequences of the rename rather than scope creep, and repo-wide verification confirms no live code path still points at the removed `containers/esm2`.</result>
<usage><subagent_tokens>64935</subagent_tokens><tool_uses>10</tool_uses><duration_ms>50914</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>aaffe787787abe480</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-protlms/bf2b7b10-35fa-4759-98a9-8aca3a54d050/tasks/aaffe787787abe480.output</output-file>
<status>completed</status>
<summary>Agent "Implement Task 10: jacobian helpers" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>## Status
**COMPLETE** — Task 10 implemented successfully via TDD.

## Commits
- `e6288b4` — esm: add pure jacobian_to_contacts + aa_token_ids helpers

## Test Summary
All 14 tests in test_esm_entrypoint.py pass (3 new + 11 existing). NEW: jacobian symmetry/zero-diag/shape validation; AA-permutation invariance; aa_token_ids mapping.

## Concerns
None. Implementation matches the brief exactly, tests are comprehensive, code passes all checks.

## Report
`/home/briney/git/protlms/.superpowers/sdd/task-10-report.md`</result>
<usage><subagent_tokens>33451</subagent_tokens><tool_uses>14</tool_uses><duration_ms>61331</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a107f445df62f4e81</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-protlms/bf2b7b10-35fa-4759-98a9-8aca3a54d050/tasks/a107f445df62f4e81.output</output-file>
<status>completed</status>
<summary>Agent "Review Task 10 (spec + quality)" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Confirmed: `numpy` is not imported at module level in the test file either — so numpy is presumably available as a dependency of the test environment already (pytest test env, not the dependency-light entrypoint module itself). The per-test `import numpy as np` (repeated 3 times, matching the brief's exact test code verbatim) is a minor style redundancy but exactly matches what the brief specified — not an implementer deviation, purely copied from spec. This is a Minor/cosmetic nit at most (could hoist to module level), but again brief-sanctioned.

I now have everything needed to write the final verdict.

### Spec Compliance
- ✅ Spec compliant
- ⚠️ Cannot verify from diff: real-world numerical agreement with the original Zhang/Ovchinnikov reference implementation (e.g. ESM-IF/`esm.utils.contacts`) beyond what the brief's pseudocode specifies — no reference fixture or golden-value test is present, so correctness rests on the pseudocode transcription itself, not an independent oracle.

Verified line-by-line against `containers/esm/entrypoint.py:113-133`:
1. Centering over all 4 axes (`for axis in range(4): j = j - j.mean(axis=axis, keepdims=True)`) — matches spec order/axes exactly (non-in-place rebind, safer than the spec's literal `-=` since it avoids mutating an aliased input array — see Strengths).
2. 4-D symmetrize `(j + j.transpose(2, 3, 0, 1)) / 2.0` — exact transpose permutation match.
3. Frobenius norm over AA axes `np.sqrt((j**2).sum(axis=(1, 3)))` → `(L, L)` — exact axis match.
4. Diagonal zeroed via `np.fill_diagonal(contacts, 0.0)` before APC.
5. APC: `a1 = contacts.sum(axis=0, keepdims=True)`, `a2 = contacts.sum(axis=1, keepdims=True)`, `contacts - (a1 * a2) / contacts.sum()` — correct broadcast orientation, computed post-diagonal-zeroing per spec order; diagonal re-zeroed after.
6. Final `(L,L)` symmetrize `(contacts + contacts.T) / 2.0`, cast to `float32` via `.astype(np.float32)`.

Numerically verified with a standalone reproduction (`np.random.default_rng` seeded inputs) that the implementation: produces the correct `(L,L)` shape, is symmetric, has a zero diagonal, and returns `float32`.

`_AA_ORDER = "ACDEFGHIKLMNPQRSTVWY"` (entrypoint.py:105) — confirmed 20 unique letters, no duplicates, exact match to spec string. `aa_token_ids` (entrypoint.py:108-110) maps each letter via `tokenizer.convert_tokens_to_ids`, returns a 20-element list — matches spec and test (`test_aa_token_ids_maps_twenty_amino_acids`, tests/test_esm_entrypoint.py:89-95) using a fake tokenizer.

**Permutation-invariance test reasoning (verified empirically, not just asserted):** I constructed several deliberately-broken variants of the pipeline to probe what the test (`tests/test_esm_entrypoint.py:76-86`) actually detects:
- Skipping the AA-axis centering, skipping the 4-D symmetrize step, using wrong axes in APC, and using `abs` instead of sum-of-squares all **still pass** the permutation-invariance test — because permuting axes 1 and 3 identically and then summing over those same axes is invariant regardless of bugs confined to the L-axes (0, 2) or the (L,L)-only APC step.
- A bug that indexes a *specific* AA channel instead of reducing over all of them (e.g., `j[:, 0, :, 0]` instead of `(j**2).sum(axis=(1,3))`) **does fail** the test (max diff ≈ 0.53).

Conclusion: the test is genuine, not vacuous, but its detection power is narrow — it only catches bugs in how the AA axes are reduced/aggregated (e.g., wrong axis choice for the Frobenius sum, or an off-by-one/asymmetric handling of axes 1 vs 3). It provides no independent signal on the correctness of the L-axis centering, the 4-D transpose symmetrization, or the APC formula — those are validated only by direct code inspection against the spec text, and by the shape/symmetry/zero-diagonal test (`tests/test_esm_entrypoint.py:63-73`). This matches the brief's own framing exactly, so the test does what it claims to do — no overclaiming.

### Strengths
- The centering step uses `j = j - j.mean(...)` (rebind) rather than the spec's literal `j -= j.mean(...)` (in-place). Since `np.asarray(jac, dtype=np.float64)` can return a view/alias of the caller's array when `jac` is already `float64` (e.g., the test's `rng.standard_normal` output), an in-place `-=` would silently mutate the caller's input array. The implementer's non-in-place version avoids this aliasing hazard while being numerically identical — a good defensive deviation from the literal pseudocode.
- `fill_diagonal` and all mutations operate only on the locally-produced `contacts` array, never on `jac` — no aliasing bugs on the input.
- Correctly follows the existing file convention of lazy-importing heavy/optional dependencies inside functions (`import numpy as np` inside `jacobian_to_contacts`, entrypoint.py:120), consistent with the module's stated "dependency-light at import time" design (entrypoint.py:10-12).
- Test suite grew from 11 to 14 tests, all passing; ruff check/format clean (independently reverified).
- `_AA_ORDER` verified programmatically: exactly 20 unique letters, correct standard alphabet.

### Issues

#### Critical (Must Fix)
None.

#### Important (Should Fix)
None.

#### Minor (Nice to Have)
- `containers/esm/entrypoint.py:108,113` — `aa_token_ids` and `jacobian_to_contacts` have no type hints on `jac`/return (`# noqa: ANN001, ANN201`), despite the file already having `from __future__ import annotations` (entrypoint.py:15), which makes annotations lazy strings — `jac: np.ndarray` / `-&gt; np.ndarray` could have been added with zero runtime cost (no need to import numpy at module level) while satisfying the project's "type hints on all function signatures" convention. This was copied verbatim from the brief's own suggested code, so it's a brief-level gap rather than an implementer error, but it's an easy improvement.
- `tests/test_esm_entrypoint.py:64,77` — `import numpy as np` is repeated inside each of the two new test functions rather than hoisted to the module-level import block (entrypoint.py test file has no top-level numpy import). Purely cosmetic; matches the brief's literal test code.
- `containers/esm/entrypoint.py:113` — no reference/golden-value test against a known-correct output (e.g., a small hand-computed or externally-sourced contact map) exists to validate the pipeline against ground truth beyond re-deriving the spec's own pseudocode; current tests validate internal consistency (shape/symmetry/zero-diag/permutation-invariance) but not absolute correctness against the original Zhang/Ovchinnikov reference output. Not required by the brief, so not weighted as Important, but worth noting as a coverage gap for future hardening.

### Assessment
**Task quality:** Approved
**Reasoning:** The implementation is a faithful, line-for-line correct port of the specified 6-step pipeline (verified by direct inspection and independent numerical reproduction), the AA alphabet mapping is correct with no duplicates, and the permutation-invariance test — while narrow in scope, as reasoned and empirically confirmed above — is genuinely meaningful rather than vacuous. All identified issues are minor/cosmetic and inherited from the brief's own suggested code rather than implementer error.</result>
<usage><subagent_tokens>43881</subagent_tokens><tool_uses>18</tool_uses><duration_ms>156564</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>adc3e8bc898a3a7c5</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-protlms/bf2b7b10-35fa-4759-98a9-8aca3a54d050/tasks/adc3e8bc898a3a7c5.output</output-file>
<status>completed</status>
<summary>Agent "Implement Task 11: cmd_contacts" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Status: DONE
Commit: cf00b79 "esm: implement contacts subcommand (categorical jacobian)"
Tests: 16/16 in test_esm_entrypoint.py pass (2 new + 14 existing); full suite 223 passed, 29 skipped (Docker-gated); confirmed torch not installed in this env, module still imports cleanly.
Concerns: `categorical_jacobian` itself is unverified (no torch here) — needs the Task 13 Docker integration test to confirm correctness end-to-end; noted in report.
Report: `/home/briney/git/protlms/.superpowers/sdd/task-11-report.md`</result>
<usage><subagent_tokens>60545</subagent_tokens><tool_uses>19</tool_uses><duration_ms>116661</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a353f91554f526292</task-id>
<tool-use-id>toolu_017A6AcWtDovgk7AkfVnZHmg</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-protlms/bf2b7b10-35fa-4759-98a9-8aca3a54d050/tasks/a353f91554f526292.output</output-file>
<status>completed</status>
<summary>Agent "Review Task 11 (spec + quality)" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Confirmed: ANN isn't in the project's `select` list, so those `noqa: ANN001, ANN201` comments are inert but consistent with the file's existing style elsewhere (pre-existing functions use the same pattern) — not a new issue introduced by this task, purely cosmetic/harmless consistency choice matching the brief's literal spec.

I've now completed a thorough trace of all spec requirements and the tensor math. Here is my final review.

### Spec Compliance
- ✅ Spec compliant
- ⚠️ Cannot verify from diff: exact numeric output of `categorical_jacobian` against a real ESM checkpoint (requires torch/transformers; deferred to Task 13's Docker integration test, as the brief and report both note).

### Strengths
- `categorical_jacobian` tensor construction is provably correct. I hand-verified it with a synthetic "model" (deterministic function of full-sequence token ids, so cross-position effects are exercised) standing in for `model(...)`: `jac[n,a,j,b]` reproduces `logit(mutate pos n to aa a).at(output pos j, aa b) - baseline.at(j,b)` exactly, and a self-substitution sanity check (mutating a position back to its own WT identity) yields exactly zero, as it must.
- `residue_pos` derivation via `tokenizer.get_special_tokens_mask(..., already_has_special_tokens=True)` (flag==0 → residue) is correct for ESM's `&lt;cls&gt; ... &lt;eos&gt;` scheme and is a more robust generalization of the hardcoded `range(1, len-1)` pattern used elsewhere in the file (e.g. `_pseudo_log_likelihood`, `_embed_one`) — verified it reduces to the same index set for the standard case.
- Sub-batching over `range(0, 20, batch_size)` and concatenation reconstructs order `0..19` correctly for every `batch_size` tested (divisors, non-divisors, and `batch_size &gt; 20`), confirmed numerically.
- `write_contacts_outputs` output (`path`, `kind: "contact_map"`, `record_ids`, `shape`, `dtype: "float32"`) and `cmd_contacts`'s `write_result` payload match `docs/CONTRACT.md`'s worked example and `tests/data/result.contacts.example.json` (a pre-existing fixture from an earlier task) byte-for-byte in structure.
- Device/autocast handling (`torch.autocast(device_type="cuda", enabled=use_amp)`, `.float().cpu().numpy()`) is consistent with the sibling `_embed_one`/`_pseudo_log_likelihood`/`_wt_position_logprobs` implementations already in the file.
- `torch`/`numpy` imported inside function bodies only, per the dependency-light-at-import-time contract; 16/16 tests pass and collect without torch installed.
- Parser subcommand and manifest capability wired exactly as specified; module docstring updated to mention `contacts`.

### Issues

#### Critical (Must Fix)
None.

#### Important (Should Fix)
None.

#### Minor (Nice to Have)
- `containers/esm/entrypoint.py:479` — `special = tokenizer.get_special_tokens_mask(input_ids.tolist(), already_has_special_tokens=True)` is a 100-char-ish line but under the repo's 100-char limit and `ruff check`/`ruff format --check` both pass clean; no action needed, noted only because it's visually dense.
- `containers/esm/entrypoint.py:472,494` — `# noqa: ANN001, ANN201` / `# noqa: ANN202` comments are inert since `ANN` rules aren't in this project's `ruff.lint.select` list (confirmed via `ruff check --select ANN`, which does flag pre-existing sibling functions like `_masked_position_logprobs`). Harmless — matches the brief's literal code and the file's pre-existing convention — but purely decorative.
- No unit test exercises the `batch_size=None → 20` default path or the sub-batching loop itself (acceptable per the brief: `categorical_jacobian` is explicitly deferred to Task 13's Docker-gated test).

### Assessment
**Task quality:** Approved
**Reasoning:** All pure-Python surfaces (parser, manifest, `write_contacts_outputs`) are correctly implemented and tested; the torch-dependent `categorical_jacobian`, which cannot be unit-tested here, was verified by hand-tracing the exact tensor construction with a synthetic cross-position-dependent stand-in model, confirming the indexing, mutation, sub-batching, and baseline-subtraction broadcast are all correct with no shape/axis errors.</result>
<usage><subagent_tokens>59471</subagent_tokens><tool_uses>22</tool_uses><duration_ms>114250</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a0d4e78e3a53bcf0f</task-id>
<tool-use-id>toolu_01EHC5N6Gs4ByyyJVZSv4xoC</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-protlms/bf2b7b10-35fa-4759-98a9-8aca3a54d050/tasks/a0d4e78e3a53bcf0f.output</output-file>
<status>completed</status>
<summary>Agent "Implement Task 12: registry entries" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>## Status: COMPLETE ✓

**Commit:** 9e318e0 — registry: add esm1b + esm2 35M/150M/3B/15B on shared esm image

**Test Summary:** 17/17 passed (3 new tests + 14 existing; 0.08s)

**Entries Added:**
- esm1b (family: esm1b, alias: esm1b_t33_650M)
- esm2-35m (family: esm2, alias: esm2_t12_35M)
- esm2-150m (family: esm2, alias: esm2_t30_150M)
- esm2-3b (family: esm2, alias: esm2_t36_3B)
- esm2-15b (family: esm2, alias: esm2_t48_15B)

**Concerns:** None. All fields match brief exactly, YAML valid, tests passing, linting clean.

**Report:** `/home/briney/git/protlms/.superpowers/sdd/task-12-report.md`</result>
<usage><subagent_tokens>32782</subagent_tokens><tool_uses>13</tool_uses><duration_ms>59519</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a73143082cd6d4809</task-id>
<tool-use-id>toolu_013Jd7pFzrEd3U8J4gyiuMb3</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-protlms/bf2b7b10-35fa-4759-98a9-8aca3a54d050/tasks/a73143082cd6d4809.output</output-file>
<status>completed</status>
<summary>Agent "Review Task 12 (spec + quality)" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Ruff is clean, confirming the report's claim. No `ty` check needed re-verification since only YAML + test file changed (no type-relevant src changes beyond data). Everything checks out.

### Spec Compliance
- ✅ Spec compliant

All 5 required entries (`esm1b`, `esm2-35m`, `esm2-150m`, `esm2-3b`, `esm2-15b`) are present in `src/protlms/_data/models.yaml` (diff lines 27–61), each with exactly the `name`, `aliases`, `image`, `model_family`, `build.context: containers/esm`, and `build.args` values dictated by the brief, verbatim:
- `esm1b`: alias `esm1b_t33_650M`, image `ghcr.io/briney/protlms-esm:esm1b_650M`, family `esm1b`, `ESM_HF_ID: facebook/esm1b_t33_650M_UR50S` — matches brief exactly.
- `esm2-35m`/`esm2-150m`/`esm2-3b`/`esm2-15b`: family `esm2`, HF ids `facebook/esm2_t12_35M_UR50D`/`_t30_150M_UR50D`/`_t36_3B_UR50D`/`_t48_15B_UR50D` — matches brief exactly, and matches the real HuggingFace facebook/ESM-2 checkpoint naming (t12=35M, t30=150M, t36=3B, t48=15B are the actual official ESM-2 layer-count/size pairings).
- Verified programmatically (loaded the YAML): 15 total model names, zero duplicate names, zero duplicate aliases, zero name/alias collisions.
- Confirmed `cf00b79` (diff base) matches the actual pre-change file state — no hidden base drift.
- The 3 new tests (`tests/test_registry.py:130-148`) correctly assert what the brief/global constraints require: `test_resolve_esm1b` checks family `esm1b`, image prefix, `build.context == "containers/esm"`, and the exact `ESM_HF_ID`; `test_registry_includes_all_esm2_sizes` checks all 6 esm2 sizes (8m/35m/150m/650m/3b/15b) are present; `test_resolve_esm2_3b_uses_shared_context` checks shared context + `ESM_MODEL_FAMILY == "esm2"`.
- Ran `pytest tests/test_registry.py -q`: 17/17 pass, matching the report. Ran `ruff check src/ tests/` and `ruff format --check src/ tests/`: both clean, matching the report.
- No ML dependencies were touched (YAML data + test file only).

- ⚠️ Cannot verify from diff: actual buildability of the images against `containers/esm`'s Dockerfile for these new `ESM_MODEL_FAMILY=esm1b` / new HF ids (i.e., whether `containers/esm`'s entrypoint correctly branches on `model_family: esm1b` vs `esm2`) — that file isn't part of this diff and wasn't reviewed here since the task is registry-only.

### Strengths
- Exact adherence to the brief's literal YAML block — no deviation in names, aliases, image tags, families, or HF ids.
- New entries reuse the established `t&lt;layer&gt;_&lt;size&gt;` image-tag convention for esm2 sizes (t12_35M, t30_150M, t36_3B, t48_15B), consistent with the pre-existing `t6_8M`/`t33_650M` entries — good consistency, not just brief-copying.
- Test additions target exactly the constraints called out in the review brief (family+HF id+context for esm1b; full esm2 size coverage; shared context for esm2-3b) rather than superficial checks.
- Entries placed contiguously with the other ESM entries in the YAML (grouped as instructed), keeping the file's logical ordering intact.
- No regressions: all 14 pre-existing tests still pass alongside the 3 new ones.

### Issues
#### Critical (Must Fix)
None.

#### Important (Should Fix)
None.

#### Minor (Nice to Have)
- `src/protlms/_data/models.yaml:19-25`: The `esm1b` image tag (`esm1b_650M`) breaks from the `t&lt;layers&gt;_&lt;size&gt;` tag convention used by every other ESM-2 entry in the file (`t6_8M`, `t33_650M`, `t12_35M`, etc.). This is exactly what the brief specified, so it's not a compliance issue, but it does mean the `esm-c` and `esm2` families share fully-parallel tag schemes while `esm1b` doesn't — worth a one-line comment or note if this asymmetry could confuse future contributors adding more esm1-family checkpoints (e.g., if ESM-1 variants beyond 650M are ever added). Not blocking.

### Assessment
**Task quality:** Approved
**Reasoning:** Every entry matches the brief and global constraints byte-for-byte, YAML is valid with no duplicate names/aliases, the 3 new tests assert exactly what was asked, and independently re-run tests/lint both pass clean.</result>
<usage><subagent_tokens>40963</subagent_tokens><tool_uses>11</tool_uses><duration_ms>55973</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a13ad4df335d305fc</task-id>
<tool-use-id>toolu_01EEg4aefS1Eaqy7BtHrUQk9</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-protlms/bf2b7b10-35fa-4759-98a9-8aca3a54d050/tasks/a13ad4df335d305fc.output</output-file>
<status>completed</status>
<summary>Agent "Implement Task 13: esm integration test" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Status: DONE
Commit: dd0d6ea "test: end-to-end esm contacts + CASP14 precision@L integration"
Test summary: image built clean; fast suite 9/9 passed (15.3s); CASP14 eval test 1/1 passed (302s, no timeout); observed T1024 precision@L = 0.0614 (391 residues, 574 true long-range contacts) — real, finite, in-range result confirming `categorical_jacobian` works end-to-end.
Full fast suite (226 tests), ruff check/format, ty check, and CLI `--help` smokes all clean.
Concerns: none.
Report: `/home/briney/git/protlms/.superpowers/sdd/task-13-report.md`</result>
<usage><subagent_tokens>44911</subagent_tokens><tool_uses>21</tool_uses><duration_ms>744149</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a8e5000caa6ff35a1</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-protlms/bf2b7b10-35fa-4759-98a9-8aca3a54d050/tasks/a8e5000caa6ff35a1.output</output-file>
<status>completed</status>
<summary>Agent "Review Task 13 (spec + quality)" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Single file changed, confirming the diff is scoped exactly as claimed (32 insertions, no other files touched, no separate un-gated test file created).

### Spec Compliance
- ✅ Spec compliant

All three tests exist verbatim as specified in the brief, appended to `tests/test_integration_esm.py` (tests/test_integration_esm.py:136-165):
1. `test_manifest_now_declares_contacts` (line 136-137) — checks `"contacts" in {c.value for c in model.manifest.capabilities}`, matching `Capability.CONTACTS = "contacts"` in `src/protlms/contract.py:28`.
2. `test_contacts_end_to_end_shapes` (line 140-149) — calls `model.contacts(TINY_FASTA, ...)`, checks `set(maps) == EXPECTED_IDS`, then per-map `(n,n)` shape, `float32` dtype, `np.isfinite(...).all()`, and `np.allclose(cmap, cmap.T, atol=1e-4)` symmetry — exactly the four properties required by the brief.
3. `test_evaluate_contacts_casp14_target` (line 152-165) — copies the real `tests/data/casp14/T1024.pdb` (verified: a genuine 2977-line PDB with 2969 ATOM records, not a stub) into a temp `pdb_dir`, calls `evaluate_contacts(model, pdb_dir, max_length=400)`, asserts `len(results) == 1`, `target_id == "T1024"`, `0.0 &lt;= precision_at_l &lt;= 1.0`, and `not math.isnan(mean_precision(results))`.

All three sit under the file's existing `pytestmark` (tests/test_integration_esm.py:36-42), which applies `pytest.mark.slow` + the `PROTLMS_RUN_DOCKER_TESTS=1`/Docker-availability `skipif` module-wide — no new gate needed, and no un-gated slow test was introduced (diff touches only this one file, confirmed via `diff --git` header count = 1).

Signature/type cross-check against source: `evaluate_contacts(model, pdb_dir, *, ..., max_length=None)` (src/protlms/eval/runner.py:47-55) accepts `max_length` as used; `TargetResult` (src/protlms/eval/runner.py:31-37) has `target_id`/`precision_at_l` fields as asserted; `mean_precision` (src/protlms/eval/runner.py:150-153) filters NaNs and only itself returns NaN if every target's precision is NaN, so the assertion is a real guard against degenerate parsing, not vacuous. `long_range_precision_at_l` is documented to return a value in `[0, 1]` or NaN (src/protlms/eval/contacts.py:131-153), so the bound assertion is correct by construction and the reported 0.0614 for a 391-residue target with 574 long-range true contacts is dimensionally plausible for an 8M-parameter model.

⚠️ Cannot verify from diff: actual pass/fail and timings (9/9 fast, 302s CASP14 run, observed P@L=0.0614) are the implementer's runtime claims and were not re-executed for this review, per instructions.

### Strengths
- Test code is a byte-for-byte match to the brief's prescribed Step 1 block — zero deviation, zero embellishment, zero weakening of assertions.
- Correctly reuses all existing module fixtures/constants (`REPO_ROOT`, `TINY_FASTA`, `EXPECTED_IDS`, `model` session fixture) rather than redefining them — tests/test_integration_esm.py:140-165.
- Eval test exercises the real fixture PDB end-to-end (copy via `read_bytes()`/`write_bytes()`, not a synthetic stand-in), giving genuine signal about the `parse_pdb` → `true_contact_map` → `long_range_precision_at_l` chain against real CASP14 data.
- Diff is minimal and correctly scoped: exactly one file touched, no source code changes, no incidental refactors.
- The lenient `0 &lt;= P@L &lt;= 1` (rather than "beats random baseline") bound is appropriately conservative for an 8M-parameter smoke test — a stricter baseline-beating assertion would be flaky given the model's small size and would conflate a science claim (does this method work) with an engineering claim (does the pipeline run correctly, which is what an integration test should verify). The comment on line 164 ("even the tiny 8M model beats a random baseline...") is slightly aspirational relative to the assertion actually being lenient, but this is not misleading since the assertion itself is not claiming that.

### Issues

#### Critical (Must Fix)
None.

#### Important (Should Fix)
None.

#### Minor (Nice to Have)
- tests/test_integration_esm.py:164 — The comment "even the tiny 8M model beats a random baseline for long-range contacts" describes a stronger property than the assertion below it verifies (`not math.isnan(...)`, not an actual baseline comparison). Not incorrect, but slightly misleading on a quick read; could be tightened to something like "guard against a fully-degenerate (all-NaN) result" to match what's actually being checked. This is inherited verbatim from the brief itself (task-13-brief.md:44), so it is not an implementer defect — flagging only for awareness, no action required on this task.

### Assessment
**Task quality:** Approved
**Reasoning:** The diff is a minimal, verbatim, correctly-gated addition of exactly the three specified tests, every referenced symbol and signature checks out against the actual source, the real CASP14 fixture is used (not a stub), and the lenient bound-only assertions are the right call for an 8M-parameter smoke test rather than a scientific benchmark claim.</result>
<usage><subagent_tokens>44984</subagent_tokens><tool_uses>12</tool_uses><duration_ms>52882</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>accea0dd02a3aed89</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-protlms/bf2b7b10-35fa-4759-98a9-8aca3a54d050/tasks/accea0dd02a3aed89.output</output-file>
<status>completed</status>
<summary>Agent "Final whole-branch review" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Confirmed: `ContactsResult` is exported, biopython is a core dep, and finding #4 is accurate — `score` (line 185) uniquely lacks `--no-pull` while every other command including the new `contacts` and `eval contacts` has it. This means the new code is actually *more* consistent than the pre-existing `score`; the inconsistency is a pre-existing gap in `score`, not a defect introduced by this branch.

I now have everything needed. Let me compile the review.

## Review Complete

### Strengths

- **End-to-end shape/dtype/key consistency is airtight.** The `contact_map` artifact contract is honored identically at every layer: container `write_contacts_outputs` writes `contacts/&lt;id&gt;.npy` with `record_ids=[id]`, `shape=[L,L]`, `dtype="float32"` (entrypoint.py:443-462); `io.load_contact_maps` keys by `record_ids[0]` and loads that path (io.py); `ContactsResult.maps()` delegates to it (models.py:126-128); and the worked example `tests/data/result.contacts.example.json` matches the `docs/CONTRACT.md` §Outputs worked example byte-for-byte. `stage_inputs` uses `seqs.fasta`, exactly matching the container's `contacts --input /in/seqs.fasta`.

- **The categorical-Jacobian science pipeline is correct.** I hand-verified the axes: `categorical_jacobian` produces `jac (n_src, a_src, j_tgt, b_tgt)` and `jac -= baseline` correctly broadcasts the `(L,20)` WT baseline over the last two axes `(j_tgt, b_tgt)` (entrypoint.py:500). `jacobian_to_contacts` implements the spec's exact 6-step order — 4-axis centering, `transpose(2,3,0,1)` symmetrization, Frobenius over the two AA axes `(1,3)`, zero-diag, APC, final symmetrization (entrypoint.py:113-133). The tokenizer-driven `aa_token_ids` + special-token-mask residue detection correctly generalizes the reference's hardcoded ESM `4:24` block.

- **Gap policy is coherent and correct.** I confirmed CASP14 T1024 has real residue-number gaps (194→203, 250→257; 391 resolved residues spanning numbers 2–406). The design is sound: `pred` (container, array-index 0..390) and `true` (`true_contact_map` from `cb_coords`, same array order) share one index space, while `resnums` maps array-index→PDB-number *only* for the separation filter `|resnum_i − resnum_j| ≥ sep`. This matches the reference's "restrict to PDB residues" and the spec exactly.

- **Contract 0.4 bump is backward-compatible.** Same major version, `extra="ignore"` on all models, `check_contract_compatibility` only enforces major-version match — a 0.3 client and 0.4 image stay mutually readable as intended. `contract.py` and `docs/CONTRACT.md` are in sync (capability, artifact kind, subcommand block, outputs row, worked example).

- **No live code path references the removed `containers/esm2`.** The directory is gone; `models.yaml` points all ESM entries at `containers/esm` / `protlms-esm`. The `protlms-esm2` strings remaining in `tests/test_runner.py`, `test_registry.py`, `test_registry_publish.py`, `test_models.py` are synthetic fixture literals, not registry lookups — harmless. The publish workflow is data-driven from `models.yaml` (no hardcoded matrix).

### Issues

#### Critical (Must Fix)
None.

#### Important (Should Fix)

- **`long_range_precision_at_l` divides by zero / returns garbage for `top&lt;=0` — reachable via the CLI (`src/protlms/eval/contacts.py:168-171`).** This is triage item #3, and I confirmed it is real by executing the metric logic in isolation:
  - `top=0` → `ZeroDivisionError: division by zero`. This is **not** a `ProtlmsError`, so the CLI's `except ProtlmsError` in `eval_contacts` (cli.py:329) will not catch it — the user gets a raw traceback instead of a clean exit-1.
  - `top=-2` → silently returns `-0.5` (`k = min(-2, size) = -2`; `order[:-2]` slices; divides by `-2`), a meaningless negative "precision."

  `protlms eval contacts ... --top 0` (or a negative) is a plausible user typo. Fix: guard at the top of the function, e.g. `if top is not None and top &lt;= 0: raise ValueError(f"top must be positive, got {top}")` (or `InvalidRequestError` so the CLI catches it cleanly). Add a parametrized test for `top=0` and `top=-1`. This is the one behavioral defect in genuinely new code with a user-facing path; everything else is cosmetic.

#### Minor (Nice to Have)

- **Doc drift: stale `containers/esm2/` / `ESM2_CHECKPOINT` references (triage #1).** Confirmed at: `docs/CONTRACT.md:327` ("See `containers/esm2/` for a reference implementation"); `CLAUDE.md:93-94`; `README.md:24,28-30,36,53`. None affect runtime, but `docs/CONTRACT.md:327` is the most visible (it's the normative contract doc's "Adding a model" section pointing at a directory that no longer exists). Recommend fixing at least the CONTRACT.md and README build-command lines. Note: `CLAUDE.md` is checked-in project instructions — updating it keeps future agents accurate.

- **`docs/CONTRACT.md` §Inputs (lines 64-68) omits `contacts` (triage #5).** The subcommand block (line 59) and Outputs table (line 145) both cover `contacts`, but the Inputs bullet list never states that `contacts` reads `/in/seqs.fasta`. One-line addition for completeness.

- **`--no-pull` inconsistency is pre-existing, not introduced here (triage #4).** `score` (cli.py:185) calls `load(model)` with no pull control, while every *other* command — including the new `contacts` (line 211) and `eval contacts` (line 302) — passes `allow_pull=False if no_pull else None`. The new code is the consistent side; `score` is the outlier. Worth a one-line fix to `score` for uniformity, but it's not a regression from this branch.

- **`esm1b` image tag `esm1b_650M` breaks the `t&lt;layers&gt;_&lt;size&gt;` convention (triage #8, `models.yaml:21`).** All ESM-2 tags are `t&lt;N&gt;_&lt;size&gt;` (e.g. `t33_650M`); ESM-1b uses `esm1b_650M`. Cosmetic — the tag is arbitrary and self-consistent with the build-arg `ESM_MODEL_NAME=esm1b_t33_650M`, but a purist would use `t33_650M` scoped by the family in the repo name. Low value; leave or normalize at will.

- **No golden-value reference test for `jacobian_to_contacts` (triage #7).** Currently validated only by internal-consistency properties (shape/symmetry/zero-diag/AA-permutation-invariance) plus the CASP14 smoke test. A tiny hand-computed 2×2 or 3×3 fixture would lock the exact numerics against future refactors. Nice-to-have, not blocking.

- **Type hints omitted on container helpers (triage #6)** via `# noqa: ANN...`. This matches the file's existing convention (all container helpers do this because `ANN` isn't in the ruff select set), so it's consistent by design — no action needed.

- **Stale plan-doc snippet (triage #2):** the plan's Task 6 Step 3 still shows the pre-fix `k = i.size if top is None else ...`; shipped code (contacts.py:168-169) is the corrected `k = n if top is None else int(top); k = min(k, i.size)`. This is a plan-doc-vs-code drift, not a code issue — the code is right.

### Recommendations

1. Fix the `top&lt;=0` guard in `long_range_precision_at_l` before merge (raise a caught, structured error) and add a regression test — this is the only functional defect.
2. Batch the doc-drift fixes (CONTRACT.md:327, README build lines, CLAUDE.md:93-94, CONTRACT.md Inputs bullet) into one small commit; they're all trivial and improve the contract doc's accuracy.
3. Optionally normalize the `score` command's `load(...)` call to accept `--no-pull` for CLI uniformity.

### Assessment

**Ready to merge?** With fixes

**Reasoning:** The architecture, cross-layer contract consistency, and the load-bearing categorical-Jacobian science (axes, baseline broadcast, post-processing order, gap policy) are all correct and I verified them by hand and by executing the metric/tensor logic. The only functional defect is the unguarded `top&lt;=0` divide-by-zero in `long_range_precision_at_l`, which is reachable via `protlms eval contacts --top` and escapes the CLI's error handling — fix that (Important) plus the cheap doc-drift items, and this is a clean merge. All other triage findings are cosmetic or pre-existing.</result>
<usage><subagent_tokens>112063</subagent_tokens><tool_uses>27</tool_uses><duration_ms>144626</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a024ae8a02df70ab4</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-protlms/bf2b7b10-35fa-4759-98a9-8aca3a54d050/tasks/a024ae8a02df70ab4.output</output-file>
<status>completed</status>
<summary>Agent "Apply final-review fixes" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Both commits made cleanly, working tree clean.

Status: DONE
Commits: d873f11 "fix(eval): reject non-positive top in precision@L"; 5c553fa "docs: fix stale containers/esm2 refs + add score --no-pull"
Tests: `test_eval_contacts.py` + `test_cli.py` = 28 passed; full fast suite `-m "not slow"` = 228 passed, 32 deselected; ruff check/format + ty check all clean
Report: `/home/briney/git/protlms/.superpowers/sdd/final-fixes-report.md`</result>
<usage><subagent_tokens>67655</subagent_tokens><tool_uses>48</tool_uses><duration_ms>185697</duration_ms></usage>
</task-notification>

---

PR merged and branch deleted. Let's move on to Plan 2 (ESM-C).

---

subagent-driven

---

<task-notification>
<task-id>ac13bb14bedc89571</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-protlms/bf2b7b10-35fa-4759-98a9-8aca3a54d050/tasks/ac13bb14bedc89571.output</output-file>
<status>completed</status>
<summary>Agent "Implement Task 1: esm-c entrypoint" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Status: DONE

Commits: `ee59e7d` — "esm-c: rewrite entrypoint on transformers + biohub weights + contacts" (branch `feature/esm-c-transformers`)

Tests: `tests/test_esmc_entrypoint.py` 13/13 passed (RED with 6 failures confirmed pre-rewrite, GREEN post-rewrite); full non-Docker suite 232 passed/17 skipped, no regressions.

Concerns: None. Note the working directory's active branch is `feature/esm-c-transformers`, not `feature/protbert-e1-encoders` shown in the initial git status — this is expected (separate worktree for this plan) and is where the commit landed.

Leftover-SDK grep: zero matches for `ESMC.from_pretrained`, `from esm.models.esmc`, `model.encode`, `ESMProtein`, `sequence_logits`, `sequence_tokens`, `MODEL_NAME`.

Report: `/home/briney/git/protlms/.superpowers/sdd/task-1-report.md`</result>
<usage><subagent_tokens>91739</subagent_tokens><tool_uses>35</tool_uses><duration_ms>190762</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a28c8bf416360e355</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-protlms/bf2b7b10-35fa-4759-98a9-8aca3a54d050/tasks/a28c8bf416360e355.output</output-file>
<status>completed</status>
<summary>Agent "Review Task 1 (spec + quality)" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>This matches the brief exactly: esmc_300m→biohub/ESMC-300M/960/30, esmc_600m→biohub/ESMC-600M/1152/36, esmc_6b→biohub/ESMC-6B/2560/80.

All checks pass. Now I have everything needed to write the final review.

### Spec Compliance
- ✅ Spec compliant

All 7 ESM-C-specific changes verified correct against `containers/esm/entrypoint.py`:
1. Module docstring — updated per brief (`transformers`/biohub weights, heavy-imports note, `ESMCForMaskedLM` registration note) — `containers/esm-c/entrypoint.py:1-14`.
2. Constants — `CONTRACT_VERSION = "0.4"` (line 25), `MAX_SEQUENCE_LENGTH = 2048` (line 26), `DEFAULT_CHECKPOINT` env-driven (line 28), `MODEL_FAMILY = "esm-c"` (line 29), `_MODEL_INFO` table (lines 33-52) — dims verified byte-exact: `esmc_300m`→960/30, `esmc_600m`→1152/36, `esmc_6b`→2560/80, hf_ids and `min_gpu_memory_gb` all match the brief.
3. `load_model` — `import esm` (line 196, `# noqa: F401`), resolves `hf_id` from `_MODEL_INFO[DEFAULT_CHECKPOINT]`, returns `(tokenizer, model)` (lines 194-204) — matches brief verbatim.
4. `build_manifest` — fully load-free, driven by `_MODEL_INFO`, `capabilities` includes `"contacts"` (lines 207-223) — matches brief verbatim.
5. `cmd_prefetch` — transformers/esm path via `_MODEL_INFO`-derived `hf_id` (lines 349-357) — matches brief verbatim.
6. `build_parser` — `prog="esm-c"` (line 598); `contacts` subparser present and unchanged from esm copy (lines 629-637).
7. `MODEL_NAME` → `DEFAULT_CHECKPOINT` — verified via AST diff: exactly 3 occurrences replaced, in `cmd_score` (line 456), `cmd_contacts` (line 550), `_write_capability_result` (line 587); zero `MODEL_NAME` references remain anywhere in the file.

No leftover SDK code — grepped for `ESMC.from_pretrained`, `from esm.models.esmc`, `model.encode`, `ESMProtein`, `sequence_logits`, `sequence_tokens`, `MODEL_NAME`: zero matches.

Faithful-copy verification (AST-level function-body diff against `containers/esm/entrypoint.py`, not just visual inspection): `sanitize_ids`, `read_fasta`, `perplexity_from_mean`, `parse_mutant`, `_valid_positions`, `_truncate`, `aa_token_ids`, `jacobian_to_contacts`, `write_result`, `emit_error_and_exit`, `pick_device`, `cmd_manifest`, `cmd_embed`, `_embed_one`, `cmd_likelihood`, `_pseudo_log_likelihood`, `_masked_position_logprobs`, `_wt_position_logprobs`, `_score_variant`, `write_contacts_outputs`, `categorical_jacobian`, `_save_npy`, `main` are byte-for-byte identical. `cmd_contacts`, `cmd_score`, `_write_capability_result` differ from the esm source by exactly the `MODEL_NAME`→`DEFAULT_CHECKPOINT` token substitution, nothing else. No function set mismatch (esm and esm-c have identical function name sets).

Heavy imports (`torch`, `transformers`, `esm`) confirmed absent from module-level AST — only `argparse`, `json`, `os`, `re`, `sys`, `pathlib.Path`, `__future__.annotations` at module scope; all ML imports are inside function bodies.

Unit tests: 10 test functions with 2 parametrize decorators (manifest ×3, mutant ×2) → 13 collected, matching the reported 13/13. Tests assert real, meaningful behavior: manifest parametrized across all 3 sizes with correct dims/contract/capabilities; jacobian shape/symmetry/zero-diagonal/dtype; `aa_token_ids` length-20 + ordering; parser has `contacts` subcommand wired to `cmd_contacts` with correct default method. No gaps versus the brief's Step 1 spec.

⚠️ Cannot verify from diff: model-dependent runtime correctness (`load_model` actually loading biohub weights, `ESMCForMaskedLM` registration working, hidden_states/logits shapes at inference time) — correctly deferred to Task 3's docker probe per the brief.

### Strengths
- Exact adherence to the brief's enumerated diff list — confirmed via automated AST function-body comparison against the esm source, not just eyeballing.
- Test suite upgrade is a genuine improvement in rigor: parametrized manifest test now covers all 3 checkpoint sizes including the newly-added `esmc_6b`, plus new jacobian/aa_token_ids/parser tests with meaningful assertions (symmetry, zero-diagonal, dtype).
- `_MODEL_INFO` table values are exactly correct (300m/600m/6b dims, hf_ids, gpu memory) — no transcription errors.
- Report includes a self-review grep for leftover SDK references and an explicit import-without-ML-stack smoke test, both of which independently reproduce clean.
- Full regression suite reported green (232 passed, 17 skipped for docker-gated tests) — consistent with the scope of this change (no other files touched).

### Issues

#### Critical (Must Fix)
None.

#### Important (Should Fix)
None.

#### Minor (Nice to Have)
- `containers/esm-c/entrypoint.py:598` — `build_parser`'s `argparse.ArgumentParser(prog="esm-c", description="ESM-C protlms contract entrypoint.")` line is 110 characters (mirrors the same pattern at `containers/esm/entrypoint.py:575`, which is 100 chars exactly due to "ESM" being 2 chars shorter than "ESM-C" twice). This is under ruff's configured `line-length = 100` and does trip E501 nominally, but `ruff check` reports "All checks passed!" on both files — worth double-checking why (likely `E501` is excluded from the configured `select` list in `pyproject.toml`, matching the pattern already tolerated for `_pseudo_log_likelihood`'s 110-char signature line in both esm and esm-c). Not a regression introduced by this task since it results directly from mechanical substitution of "esm"→"esm-c"/"ESM"→"ESM-C", and ruff is clean either way — flagging only for awareness, no fix required.

### Assessment
**Task quality:** Approved
**Reasoning:** Every one of the 7 specified changes is applied exactly as directed, verified via AST-level function-body diffing (not just visual inspection) confirming zero unintended drift in the 22 functions required to be byte-identical and the 3 functions requiring only the `MODEL_NAME`→`DEFAULT_CHECKPOINT` substitution. No leftover SDK references, no module-level heavy imports, `_MODEL_INFO` dims are transcribed correctly, and the test suite exercises real behavior across all three checkpoint sizes plus the new contacts machinery.</result>
<usage><subagent_tokens>82697</subagent_tokens><tool_uses>15</tool_uses><duration_ms>95142</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a30d5c74e07550971</task-id>
<tool-use-id>toolu_015u1ym9UjBUs17DUd1VrdYp</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-protlms/bf2b7b10-35fa-4759-98a9-8aca3a54d050/tasks/a30d5c74e07550971.output</output-file>
<status>completed</status>
<summary>Agent "Implement Task 2: esm-c Dockerfile + registry" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Status: DONE

Commits: `5566311` esm-c: transformers/biohub Dockerfile (pinned esm) + esm-c-6b registry entry

Tests: RED confirmed (2 new tests failed pre-change) then GREEN (`3 passed, 16 deselected`); full suite `234 passed, 32 skipped` (skips are pre-existing Docker/GHCR integration tests requiring a daemon, unaffected by this task).

Concerns: None — brief followed verbatim; Dockerfile diff matches Step 6 exactly (SHA substituted); 300m/600m registry entries left untouched; `entrypoint.py` from Task 1 already has `esmc_6b` in `_MODEL_INFO`, so no gaps. Note: pre-existing stale `task-2-report.md` from an earlier/different plan numbering was overwritten with this task's report per my assignment.

Report: `/home/briney/git/protlms/.superpowers/sdd/task-2-report.md`</result>
<usage><subagent_tokens>65704</subagent_tokens><tool_uses>28</tool_uses><duration_ms>154179</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>aecaf75b0f7d40d41</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-protlms/bf2b7b10-35fa-4759-98a9-8aca3a54d050/tasks/aecaf75b0f7d40d41.output</output-file>
<status>completed</status>
<summary>Agent "Review Task 2 (spec + quality)" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>This confirms the removed comment (explaining "The esm package requires Python 3.12... this base differs from ESM2's pytorch base... flash-attn is intentionally not installed") is gone from the Dockerfile header, though it's not clear this rationale still holds for the biohub package (the constraint was tied to the old EvolutionaryScale `esm==3.2.3` SDK, not necessarily the new biohub git package). This is minor since the README restates the flash-attn note and Python 3.12 requirement in the Notes section. Not a spec violation — the brief didn't mandate keeping that comment.

Now I have all findings. Let me compile the review.

### Spec Compliance
- ✅ Spec compliant

All items verified directly against the diff:
- Base image: `ARG BASE_IMAGE=python:3.12-slim-bookworm` unchanged (`containers/esm-c/Dockerfile:16`).
- `ARG ESM_REF=cf002f1d2fb9220a0918fc8c5ea4e3b56d28fbaf` present, matches the mandated SHA exactly (`containers/esm-c/Dockerfile:21`).
- Install line: `RUN pip install --no-cache-dir "esm @ git+https://github.com/Biohub/esm.git@${ESM_REF}"` (`containers/esm-c/Dockerfile:30`) — matches spec exactly, no version pin/extras added.
- apt install: `RUN apt-get update &amp;&amp; apt-get install -y --no-install-recommends git build-essential \` followed by `&amp;&amp; rm -rf /var/lib/apt/lists/*` in the same layer (`containers/esm-c/Dockerfile:28-29`) — single RUN, cache cleaned.
- `RUN python /app/entrypoint.py _prefetch` present (`containers/esm-c/Dockerfile:36`), placed **before** `ENV HF_HUB_OFFLINE=1` (`containers/esm-c/Dockerfile:39`) — correct ordering (offline flag only takes effect at runtime, after the image layer with cached weights is baked).
- No `transformers` version pin anywhere in the Dockerfile (grep of diff confirms only the `esm` package is pinned) — spec compliant; comment at line 27 correctly notes "~4.57.x" as documentation only, not a pin.
- `HF_HOME=/opt/hf-cache` retained in the `ENV` block (`containers/esm-c/Dockerfile:24`).
- Registry: `esm-c-6b` entry (`src/protlms/_data/models.yaml:75-81`) matches spec fields exactly — name, `aliases: [esmc_6b]`, `image: ghcr.io/briney/protlms-esm-c:6b`, `model_family: esm-c`, `build.context: containers/esm-c`, `build.args: { ESMC_CHECKPOINT: esmc_6b }`.
- `esm-c-300m` (`models.yaml:61-67`) / `esm-c-600m` (`models.yaml:68-74`) blocks are untouched by the diff (diff shows only an insertion after `esm-c-600m`, no `-`/context lines touching those two blocks).
- Two new registry tests match the brief's Step 1 verbatim: `test_resolve_esm_c_6b` and `test_registry_includes_all_esm_c_sizes` (`tests/test_registry.py:72-83`), and the pre-existing `test_resolve_esm_c` (300m/600m) is untouched.
- README rewritten to the transformers/biohub path (`containers/esm-c/README.md:1-70`): drops the `esm==3.2.3` SDK / EvolutionaryScale-only framing, documents the pinned git install, adds `contacts` capability, adds the `esmc_6b` build command and models-table row, updates the license framing to MIT/biohub for all three sizes.

⚠️ Cannot verify from diff (explicitly out of scope per task): actual Docker build success, whether `pip install "esm @ git+..."` at the pinned SHA resolves without conflicts, whether the biohub weights actually download at that commit. Task 3's job, correctly deferred.

### Strengths
- The `ESM_REF` pin and its accompanying `git ls-remote` re-resolution comment (`containers/esm-c/Dockerfile:19-20`) mirror the established E1 pattern (`containers/e1/Dockerfile:20-21`) almost exactly — good consistency across containers.
- `HF_HUB_OFFLINE=1` ordering is correct and matches the working E1 precedent line-for-line.
- The registry entry insertion is minimal and surgical — a pure addition after `esm-c-600m`, verified byte-identical siblings.
- Tests exercise both the canonical name and alias resolution plus `build.context`/`build.args`, consistent with the existing `test_resolve_esm_c` style.
- README changes are thorough and consistent with the entrypoint's actual `_MODEL_INFO` table (`esmc_6b`: 2560 embedding_dim / 80 layers matches `containers/esm-c/entrypoint.py:46-51` exactly) and actual capability list (`embed`, `likelihood`, `score`, `contacts` matches `build_manifest()` at `containers/esm-c/entrypoint.py:216`).
- No transformers version pin was introduced anywhere, correctly honoring the constraint.

### Issues

#### Critical (Must Fix)
None.

#### Important (Should Fix)
None.

#### Minor (Nice to Have)
- `containers/esm-c/Dockerfile:1-14` — the old header comment explaining the Python-3.12 requirement and the flash-attn omission rationale was dropped entirely rather than adapted; that context now lives only in the README's Notes section (`containers/esm-c/README.md:59,69`). Not a spec violation (the brief's Step 6 block explicitly does not include this text), but a future reader of just the Dockerfile loses the "why 3.12 / why no flash-attn" rationale inline. Low priority since the README carries it.
- `containers/esm-c/Dockerfile:27` — the comment "esm pulls torch + transformers (~4.57.x)" is a documentation claim that isn't verified anywhere in this diff (no `pip freeze`/build was run, correctly deferred to Task 3); harmless but worth a follow-up sanity check once Task 3 builds the image, to make sure the comment stays accurate.

### Assessment
**Task quality:** Approved
**Reasoning:** Every mandated Dockerfile detail (base image, `ESM_REF` pin, install line, apt cleanup, prefetch-before-offline ordering, no transformers pin) and registry/test/README requirement is met exactly as specified, with the two untouched sibling entries verified byte-identical in the diff; only trivial documentation nits remain.</result>
<usage><subagent_tokens>59849</subagent_tokens><tool_uses>10</tool_uses><duration_ms>42720</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a63bef161ca1e9358</task-id>
<tool-use-id>toolu_01CGN5Vq2JewypaesuGaJRGi</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-protlms/bf2b7b10-35fa-4759-98a9-8aca3a54d050/tasks/a63bef161ca1e9358.output</output-file>
<status>completed</status>
<summary>Agent "Implement Task 3: esm-c integration" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>The Docker build (esm-c 300M image) is still in progress in the background. I've set up both a Monitor watcher and a background bash job to detect completion. I'll wait for that notification before proceeding to the probe step, entrypoint verification, test updates, and final test run.

I'll pause my turn here and resume once the build-completion notification arrives.</result>
<usage><subagent_tokens>84199</subagent_tokens><tool_uses>44</tool_uses><duration_ms>174945</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>bv3get4c4</task-id>
<tool-use-id>toolu_01WNhSM1kA1N8qDaZSbeDdf3</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-protlms/bf2b7b10-35fa-4759-98a9-8aca3a54d050/tasks/bv3get4c4.output</output-file>
<status>completed</status>
<summary>Background command "cd /home/briney/git/protlms
BUILDLOG=/tmp/claude-1001/-home-briney-git-protlms/bf2b7b10-35fa-4759-98a9-8aca3a54d050/scratchpad/build.log
for i in $(seq 1 100); do
  if docker image inspect ghcr.io/briney/protlms-esm-c:300m &gt;/dev/null 2&gt;&amp;1; then
    echo "IMAGE_READY after ~$((i*20))s"; exit 0
  fi
  if ! pgrep -f "docker.*build.*protlms-esm-c|buildx build.*protlms-esm-c" &gt;/dev/null 2&gt;&amp;1; then
    # build process gone and no image =&gt; failed
    echo "BUILD_DIED_NO_IMAGE after ~$((i*20))s — last build.log lines:"
    tail -40 "$BUILDLOG" 2&gt;/dev/null
    exit 2
  fi
  sleep 20
done
echo "TIMEOUT after ~2000s; build still running or stuck. last build.log:"
tail -20 "$BUILDLOG" 2&gt;/dev/null
exit 1" completed (exit code 0)</summary>
</task-notification>

---

<task-notification>
<task-id>a63bef161ca1e9358</task-id>
<output-file>/tmp/claude-1001/-home-briney-git-protlms/bf2b7b10-35fa-4759-98a9-8aca3a54d050/tasks/a63bef161ca1e9358.output</output-file>
<status>completed</status>
<summary>Agent "Implement Task 3: esm-c integration" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>I'll pause and wait for the build completion notification.</result>
<usage><subagent_tokens>114442</subagent_tokens><tool_uses>70</tool_uses><duration_ms>768054</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>bylcvg3pz</task-id>
<tool-use-id>toolu_018UdFujfPhFCNwQKCe6zUF8</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-protlms/bf2b7b10-35fa-4759-98a9-8aca3a54d050/tasks/bylcvg3pz.output</output-file>
<status>completed</status>
<summary>Background command "cd /home/briney/git/protlms
OLD=sha256:c51a66d9ea40f2771c17189d1da315e2f82c4ae11d39a49e9a12c77067cd0a64
for i in $(seq 1 100); do
  NEW=$(docker image inspect ghcr.io/briney/protlms-esm-c:300m --format '{{.Id}}' 2&gt;/dev/null)
  if [ -n "$NEW" ] &amp;&amp; [ "$NEW" != "$OLD" ]; then echo "REBUILD_DONE new_id=$NEW after ~$((i*20))s"; exit 0; fi
  if ! pgrep -f "docker.*build.*protlms-esm-c|buildx build.*protlms-esm-c" &gt;/dev/null 2&gt;&amp;1; then
    if [ -n "$NEW" ] &amp;&amp; [ "$NEW" != "$OLD" ]; then echo "REBUILD_DONE(late) $NEW"; exit 0; fi
    echo "BUILD_GONE_STILL_OLD_OR_ABSENT after ~$((i*20))s (id=$NEW)"; exit 2
  fi
  sleep 20
done
echo "TIMEOUT ~2000s"; exit 1" completed (exit code 0)</summary>
</task-notification>

---

<task-notification>
<task-id>a63bef161ca1e9358</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-protlms/bf2b7b10-35fa-4759-98a9-8aca3a54d050/tasks/a63bef161ca1e9358.output</output-file>
<status>completed</status>
<summary>Agent "Implement Task 3: esm-c integration" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Waiting for the pytest completion notification. Once it arrives I'll examine the embed_pooled failure and the contacts result, then diagnose and act.</result>
<usage><subagent_tokens>116944</subagent_tokens><tool_uses>9</tool_uses><duration_ms>50872</duration_ms></usage>
</task-notification>

---

Why are we running the 300M test on CPU? You have a GPU in your local environment...