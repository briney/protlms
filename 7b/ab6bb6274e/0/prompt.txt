OK, but let's commit + push the existing E1/ProtBERT changes (along with the name change. I'll pull that into main and we can start with a fresh branch off of the fully merged main.

---

PR merged and branch deleted. Ready to go.